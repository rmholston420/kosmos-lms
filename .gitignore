# Kosmos runtime
*.db
*.sqlite
*.sqlite3
*.pyc
__pycache__/
.venv/
venv/
.env
.env.*
!.env.example

# Editor
.vscode/
.idea/
*.swp
.DS_Store

# OS
Thumbs.db

# Test / build artifacts
.pytest_cache/
*.egg-info/
build/
dist/

# Kosmos benchmarks / ephemeral
ops/benchmarks/*.tmp
ops/scratch/

# Tektos eval caches (Stage 3.8+; Stage 3.9 DeepSWE hydration lands here)
.eval-cache/
plugins/tektos/eval/tasks/**/_pier_jobs/

# Vendor archives (keep vendored code, drop tarballs)
*.tar.gz
*.zip

# ADR-010 eval: keep vendored code, exclude model weights (live in HF cache)
vendor/adr_010/**/*.safetensors
vendor/adr_010/**/*.gguf
vendor/adr_010/**/*.bin
vendor/adr_010/**/*.pt
vendor/adr_010/**/*.pth

# ADR-010 eval artifacts (per-run JSON; pushed manually from Colossus)
# Uncomment the next line to also ignore artifacts:
#ops/benchmarks/artifacts/

# Perplexity Computer local skill copies (kept via .perplexity/)
skills/user/

# Kosmos secrets — private keys, sealed secrets material
.secrets/
.playwright-mcp/
.venv-eval/
ui/node_modules
ui/.next
ui/out
ui/test-results
ui/playwright-report
ui/.playwright
ui/next-env.d.ts
ui/tsconfig.tsbuildinfo
node_modules/

# Playwright output (root-level run artifacts)
test-results/

# kosmos DB credentials (gitignored; never commit)
ops/systemd/kosmos.pg.password
ops/systemd/kosmos-kernel.local.env
