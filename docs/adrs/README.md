# Kosmos ADR Index

All Architecture Decision Records for Kosmos v25. Newer ADRs supersede older ones only when explicitly stated.

**Status legend:** `Ratified` = decided and load-bearing · `Ratified v25` = newly ratified in this cut · `OPEN` = requires resolution before named lock-in phase · `N/A` = not applicable in greenfield · `Amended` = superseded scope

| ID | File | Title | Status | Lock-in Phase |
|---|---|---|---|---|
| ADR-001 | `ADR-001-typed-claim-graph-memory.md` | Typed Claim-Graph Memory + Grounded Evaluator | Ratified | Gnosis Phase 3 |
| ADR-002 | `ADR-002-gnosis-humanities-scope.md` | Gnosis absorbs Gnoma/humanities scope | Ratified | 6.6 |
| ADR-002 (supplement) | `ADR-002-supplement-humanities-detail.md` | Humanities implementation details | Ratified | 6.6 |
| ADR-003 | `ADR-003-beads-taskstate-reference.md` | Beads as TaskState design reference | Ratified | Tektos Phase 3 |
| ADR-004 | `ADR-004-bernstein-janitor-spike.md` | Bernstein Janitor spike-test | **Ratified v25 (spike approved)** | Tektos Phase 4 |
| ADR-005 | `ADR-005-openspec-primary.md` | OpenSpec as primary SDD engine | Ratified · amended by ADR-040 | Tektos Phase 3 |
| ADR-006 | `ADR-006-pier-eval-harness.md` | Pier eval-on-deploy | Superseded by ADR-042 | Tektos Phase 4 |
| ADR-007 | `ADR-007-events-only-cross-plugin-coupling.md` | Events-only cross-plugin coupling | Ratified (foundational) | Stage 1 |
| ADR-007-DeepSWE | `ADR-007-DeepSWE-corpus.md` | DeepSWE as eval-corpus candidate (manifest-only, 5-task subset landed Stage 3.9; clause 3 deferred pending context-rot regression suite) | Ratified v25 | Stage 3.9 |
| ADR-008 | `ADR-008-superpowers-kb-reference.md` | Superpowers as KB methodology reference | Ratified | Tektos Phase 4 |
| ADR-008-DozerDB | `ADR-008-DozerDB-memory-port.md` | DozerDB fork as MemoryPort store | **Ratified v25** | Stage 1 |
| ADR-009 | `ADR-009-llama-swap-primary.md` | llama-swap primary + router-mode fallback | **Ratified v25 (contingent)** | Stage 1 (benchmark-gated) |
| ADR-010 | `ADR-010-zetesis-inner-loop-eval.md` | AREX vs. Open Deep Research inner loop — **Winner: Open Deep Research** (MIT, qwen2.5:32b + Ollama + langchain-mcp over SearXNG). AREX-Turbo rejected for Stage 6.2 (0/3 completion, thermal-blank on 65k retry); on-shelf pending Colossus thermal remediation. Six trials at `ops/benchmarks/artifacts/adr-010-2026-07-30/`. | **LOCKED 2026-07-30** | Stage 6.2 |
| ADR-011 | `ADR-011-a2a-sdk-koinonia-transport.md` | a2a-sdk standalone transport for Koinonia | **Ratified v25** | Phase 6.3 |
| ADR-012 | `ADR-012-donor-adapter-consolidation.md` | Consolidate ollama.py/searxng.py duplicates | **Ratified v25** | Stage 1.1 |
| ADR-013 | `ADR-013-memory-bridge-selection.md` | Choose memory/bridge.py vs. Gnosis schema | **LOCKED** · Gnosis schema (6/6) · 2026-07-29 | Stage 1.9 |
| ADR-014 | `ADR-014-ui-parity-rule.md` | UI Parity standing rule | Ratified (v24) | Every phase after Tektos Phase 2 |
| ADR-015 | `ADR-015-oikos-before-zetesis.md` | Oikos ahead of Zetesis sequencing (**Amended 2026-07-30:** Stage-5 deferred by user; Stage 6.1 lands first — see ADR-052) | Ratified (v24) · Amended 2026-07-30 | Stage 5 (deferred) |
| ADR-016 | `ADR-016-knowsys-gnosis-merge.md` | Knowsys merged into Gnosis | **LOCKED** (2026-07-30) | Phase 3.3 (Stage 4.1) |
| ADR-017 | `ADR-017-llm-council-reference.md` | karpathy/llm-council as design reference only | Ratified | Phase 6.4 |
| ADR-018 | `ADR-018-oikos-benefit-references.md` | sure/Maybe rejected; CMSgov/18F as references | Ratified | Phase 5.3 |
| ADR-019 | `ADR-019-approval-ux.md` | Approval UX specification | Ratified | Phase 3 |
| ADR-020 | `ADR-020-tektohs-migration.md` | TektOHs v18 → Tektos v1 migration | Ratified (N/A greenfield) | Tektos Phase 3 |
| ADR-021 | `ADR-021-searchport-introduction.md` | Introduce SearchPort as 11th formal port | **Ratified v25** | Stage 1.1 |
| ADR-022 | `ADR-022-llmport-surface-expansion.md` | LLMPort surface expansion (spec §4.1 tightening) | **Ratified v25** | Stage 1.2 |
| ADR-023 | `ADR-023-eventbusport-envelope-first-mvp.md` | EventBusPort envelope-first MVP; `ack` deferred to ADR-024 | **Ratified v25** | Stage 1.4 |
| ADR-024 | `ADR-024-secretsport-age-file-backend.md` | SecretsPort adopts age-encrypted file backend; Vault + `lease()` deferred | **Ratified v25** | Stage 1.5 |
| ADR-025 | `ADR-025-observabilityport-otel-prometheus-structlog.md` | ObservabilityPort adopts OpenTelemetry SDK + prometheus-client + structlog; Langfuse LLM-specific adapter deferred | **Ratified v25** | Stage 1.6 |
| ADR-026 | `ADR-026-vectorport-qdrant-backend.md` | VectorPort adopts Qdrant backend; pgvector fallback deferred; port-level §7 zero-trust enforcement (`provenance` + `confidence` required on writes) | **Ratified v25** | Stage 1.7 |
| ADR-027 | `ADR-027-memoryport-dozerdb-graphiti-amg.md` | MemoryPort full surface (write_event / query_temporal / link_entities / quarantine_write); DozerDB (ADR-008) + Graphiti + Agent Memory Guard v0.2.2 all vendored at Stage 1.8; port-level §7 zero-trust guard is non-bypassable; AMG runs as second policy layer | **Ratified v25** | Stage 1.8 |
| ADR-028 | `ADR-028-dataport-jsonld-canonical-export.md` | DataPort full surface (export_canonical / check_format_health / migrate_schema); JCS (RFC 8785, `rfc8785` Apache-2.0) canonicalization + pluggable `Signer` Protocol seam (`NoOpSigner` primary at Stage 1.10; `Ed25519FileSigner` deferred to Stage 5 governance-key wiring via `cryptography>=49` Apache-2.0 OR BSD-3); filesystem storage with Restricted-tier routing prefix; port-level zero-trust guard is non-bypassable; never-overwrite migration guard live | **Ratified v25** | Stage 1.10 |
| ADR-029 | `ADR-029-resourceport-apex-substrate-priority-queue.md` | ResourcePort full surface — APEX substrate (six canonical kinds: time/money/attention/compute/knowledge/energy) + priority queue (fixed order per §172: Phrouros anomaly > Tektos active > Synedrion/Zetesis background); SQLite primary (WAL) via `aiosqlite>=0.20` (MIT) + pluggable `Storage` Protocol seam (`InMemoryStorage` test double); Decimal balance precision preserved end-to-end; port-level zero-trust guard is non-bypassable; over-subscription rejection (Build-Sequence §1.13 DoD: 40 GB VRAM on 32 GB card → clean rejection) | **Ratified v25** | Stage 1.11 |
| ADR-030 | `ADR-030-notificationport-algedonic-channel.md` | NotificationPort full surface — `KernelNotificationAdapter` primary + pluggable `Sink` Protocol seam (`InProcessSink` 200-cap ring buffer matching Rigpa `NotificationCenterService` donor + `NtfySink` lazy-httpx-import stub with 0.4s timeout); `AlgedonicTier` enum {INFO, WARN, ACTION, ALGEDONIC}; port-level zero-trust guard is non-bypassable; algedonic fast-path via `asyncio.gather` fan-out; `check_delivery_slo()` self-probe returns p50/p95/p99/max + 500ms-breach count (Build-Sequence §1.12 DoD: priority alert <500ms end-to-end); SMS mobile-fallback deferred to spec §344.4 (requires Stage 5 governance-key wiring) | **Ratified v25** | Stage 1.12 |
| ADR-031 | `ADR-031-frontendcontractport-declarative-ui-schema.md` | FrontendContractPort full surface — `KernelFrontendContractAdapter` primary + pluggable `ManifestStore` Protocol seam (`InMemoryManifestStore` dict-backed primary; `FileManifestStore` stdlib `pathlib`+`json` atomic-write stub, deferred to Stage 5 auditor wiring); mirrors Rigpa-LMS `RigpaFrontendPlugin` donor shape (name/state_namespace/design_tokens/routes) extended with typed `Panel` value objects across nine `PanelSlot`s (§280 + §17.9 + §17.13) + `version`/`kernel_compat`; `UiParityStatus` enum {NOT_STARTED, IN_PROGRESS, COMPLIANT, GRANDFATHERED}; port-level zero-trust guard is non-bypassable and rejects missing/invalid required fields, invalid plugin-name regex, empty route/panel `lazy_module`, and duplicate registrations; design-token merge is last-registered-wins; panel ordering is `priority DESC` with insertion-order tiebreaker; `render_kernel_schema()` returns `KernelSchema(title="Kosmos", plugins=(), panels=())` on empty registry (Build-Sequence §1.14 DoD literal anchor) | **Ratified v25** | Stage 1.14 |
| ADR-032 | `ADR-032-praxis-constitution-loader.md` | Praxis Constitution Loader — boot-time load-and-verify of ratified `vNNNN.{yaml,json,sig}` triplet under `governance/constitution/versions/` against `governance/constitution/pubkey.pem`; Ed25519 JWS over JCS canonical JSON (RFC 8785); Q1=B (verifier + loader + standalone `signing.py` helper) with amend service / CLI / models / schemas / service.py all deferred to Synedrion (Phase 6.3) per spec §278; Q2=A (Praxis registers `PluginDescriptor` with `panels=(governance,)` and `ui_parity_status=IN_PROGRESS` — no §17.1 amendment); three-tier boot invariant chain (existence → YAML/JSON JCS cross-check → Ed25519 signature verify); tamper → `ConstitutionTamperError` from `ConstitutionLoader.__init__` = boot refused; ADR-007 + ADR-008 respected | **Ratified v25** | Stage 2.1 |
| ADR-033 | `ADR-033-apex-change-approval-tier-engine.md` | APEX Change Approval Tier engine — kernel-wide `ChangeApprovalProtocol` (async `propose`/`resolve`/`list_pending`/`get_by_id`/`list_by_intention`) implementing spec §14 three-tier ladder (AUTONOMOUS auto-approves; HUMAN_REVIEW = one-shot ACTION notification + 4h missed-review timer → `apex.review.missed`; HUMAN_REQUIRED = escalating `deliver_algedonic()` cadence at T+24h then every 6h up to 30-day self-refreshing horizon); Q1=C (full §17.13 UX including SecretsPort-backed Ed25519 `MobileTokenService` — 24h TTL, JCS + b64url wire format, tamper-first verify, logical secret name `apex.approval.mobile_token.signing_key`); Q2=A (Scheduler Protocol seam — `InProcessScheduler` primary + `FakeScheduler` deterministic double + `NullScheduler`); Storage Protocol seam (`InMemoryStorage` primary + `SqliteStorage` stub for Stage 5); events envelope-first with `producer_plugin="praxis"` (ADR-023); `EscalationPolicy` scaffold classifies all nine §14 triggers; `PraxisPlugin` gains second `Panel(id="praxis.approvals", slot=APPROVALS_QUEUE)`; ADR-007 + ADR-008 respected (no cross-plugin imports, no MemoryPort writes at 2.2); ADR-024 SecretsPort backs the mobile signing key; DoD literal `pytest -k apex_tiers` — 82 contract tests green | **Ratified v25** | Stage 2.2 |
| ADR-034 | `ADR-034-phrouros-anomaly-detector.md` | Phrouros anomaly detector (Stage 2.3) — System-4 anomaly-detection plugin producing typed anomalies into the algedonic bus; new **TraceFeedPort** (`ports/trace_feed.py`) as Protocol-only seam with `InMemoryTraceFeedAdapter` primary (pure asyncio pub/sub, snapshot-list fan-out, idempotent `close`) + `LangfuseTraceFeedAdapter` stub (Stage 5); Q1=A (NotificationPort-direct algedonic — no APEX `propose()`); Q2=A (new port — do NOT amend writer-only ObservabilityPort); Q3=B (real `LoopDetector` + three skeletons: `ModelSwapSloDetector` §172 / `StubDegradationDetector` §273 / `BusFactor1Detector` §613 raising `DetectorNotImplementedError`); Q4=A (ResourcePort compute-reservation: `allocate(COMPUTE, 32 GB, priority=PHROUROS_ANOMALY)` with `ResourceExhausted` → `enqueue()` fallback); Q5=A (`PhrourosPlugin` descriptor with `Panel(slot=AGENT_TRACE, priority=100)`); events envelope-first with `producer_plugin="praxis"` (ADR-023); ADR-007 + ADR-008 respected; DoD literal `pytest -k phrouros_loop` — 55 contract tests green | **Ratified v25** | Stage 2.3 |
| ADR-035 | `ADR-035-stage-2-exit-gate-anomaly-bridge.md` | Stage-2 exit gate (Stage 2.4) — unauthorized-action → Phrouros detects → APEX escalates → user notified end-to-end scenario. Q1=A (test-only Tektos stub `plugins/tektos/stub/TektosSimulator`, deleted-or-superseded at Stage 3); Q2=C (both detectors: reuse `LoopDetector` + new real `UnauthorizedToolDetector` with hardcoded `frozenset[str]` allowlist; new `AnomalyKind.UNAUTHORIZED_TOOL`); Q3=A (event-only ADR-007 coupling via `plugins/praxis/apex/bridge.py::AnomalyBridge` — subscribes `phrouros.anomaly.detected`, translates to `ChangeApprovalProtocol.propose(tier=HUMAN_REQUIRED, proposing_domain="phrouros")`, publishes `praxis.escalation.proposed` for audit, idempotent start/stop); Q4=A (hardcoded `frozenset[str]` allowlist — no constitution schema extension, no new port; `PolicyPort` seam deferred to Stage 5); Q5=A (bridge Praxis-internal at `plugins/praxis/apex/bridge.py` — composes `ChangeApprovalProtocol` directly, not a new plugin); Q6=A (`TektosSimulator` no descriptor / no lifecycle / no panel — pure dataclass over `TraceFeedPort`); every anomaly → `HUMAN_REQUIRED` at 2.4 (per-kind tier routing deferred to Stage 3+); ADR-007 respected (AST-verified `test_bridge_never_imports_phrouros`); ADR-008 respected (no MemoryPort writes at 2.4); ADR-023 respected (bridge audit envelopes carry `producer_plugin="praxis"`); DoD literal `pytest -k stage_2_4_exit_gate` — 6 gate tests green; 598 total contract tests green | **Ratified v25** | Stage 2.4 |
| ADR-036 | `ADR-036-tektos-openhands-sdk-vendoring.md` | Tektos OpenHands SDK vendoring (Stage 3.1) — pattern-vendored `OpenHands/software-agent-sdk` (MIT) `Agent`/`Conversation` surface rewritten behind `LLMPort` + `MemoryPort` in `plugins/tektos/agent.py::TektosAgent`. Q1=A (SDK repo only; main OpenHands runtime deferred to 3.2); Q2=A (PATTERN-VENDORED — no upstream source copied); Q3=A (minimal-loop DoD: instantiate agent with fake ports, `send_message` + `run` completes one iteration reading context via `query_temporal`, calling `generate_text` once, writing back via `write_event` with fixed `provenance="tektos_agent"` + confidence in (0,1]); Q4=B (no `PluginDescriptor` at 3.1 — spec §17.1 Phase-2 grandfathering; FrontendContractPort registration lands Stage 3.7); Q5=B (`plugins/tektos/stub/TektosSimulator` alive through 3.1, deleted at 3.2); Q6=A (author new ADR-036, not amend ADR-020); locked constants `TEKTOS_AGENT_PROVENANCE="tektos_agent"` + `TEKTOS_MEMORY_PREDICATE="tektos.turn.completed"`; default confidence 0.75; ADR-007 respected (AST-verified `test_tektos_agent_imports_no_other_plugins_adr_007`); ADR-008 respected (zero-trust guard passthrough test); ADR-022 respected (consumes `LLMPort.generate_text` only); DoD literal anchor `pytest plugins/tektos/tests/test_tektos_agent.py::test_tektos_agent_reads_and_writes_via_kosmos_ports_only_build_sequence_3_1_dod` — 18 contract tests green; 616 total green | **Ratified v25** | Stage 3.1 |
| ADR-037 | `ADR-037-tektos-mcp-transport-playwright-apex-tool-gating.md` | Tektos MCP transport + Playwright-MCP + APEX tool-call gating + stub deletion (Stage 3.2) — MCP python-sdk PATTERN-VENDORED behind new `MCPPort` at `ports/mcp.py`; `InProcessMCPAdapter(FakePlaywrightServer)` for deterministic DoD + env-gated `KOSMOS_STAGE_32_REAL_PLAYWRIGHT=1` `StdioMCPAdapter` against real `@playwright/mcp`; every `TektosAgent.call_tool` proposes via `ApprovalGatewayPort` with tier from hardcoded `TEKTOS_TOOL_TIER_MAP` (fail-closed `DEFAULT_TIER=HUMAN_REQUIRED`, AUTONOMOUS auto-approves, HUMAN_REVIEW/REQUIRED raise `TektosToolCallPending`; trace-first `TraceEvent` emission BEFORE APEX gate for Phrouros observation; MemoryPort write per successful call with `predicate=TEKTOS_TOOL_PREDICATE="tektos.tool.completed"`); Q4=A deletes `plugins/tektos/stub/` per ADR-036 Q5=B trigger — Stage-2.4 exit-gate test rewired to real Tektos agent + fake MCP; Q5=A ships new `MCPPort` + amends ADR-033 by promoting `ChangeApprovalTier` + narrow propose-only `ApprovalGatewayPort` Protocol to `ports/approval.py` (Tektos imports only from `ports.*` per ADR-007; `plugins/praxis/apex/tier.py` re-exports for backwards compat); Q6=A single ADR covers all six decisions; locked constants `MCP_PROTOCOL_VERSION="2024-11-05"` + `TEKTOS_TOOL_PREDICATE="tektos.tool.completed"`; ADR-007/008/022 respected; ADR-033 amended in-flight; ADR-035 preserved; ADR-036 fulfilled at Q5=B trigger; DoD literal anchor `pytest plugins/tektos/tests/test_tektos_mcp.py::TestStage32DoD::test_browser_navigate_end_to_end_autonomous` — 8 Tektos-MCP + 12 in-process + 9 stdio + 5 rewired Stage-2.4 gate tests green; 644 total green + 2 env-gated skips | **Ratified v25** | Stage 3.2 |
| ADR-038 | `ADR-038-aider-repomap-pattern-vendor.md` | Aider repomap PATTERN-VENDORED for Tektos (Stage 3.3) — Q1=A pattern-vendor at `plugins/tektos/repomap/{policy,tags,rank,render,indexer}.py` (only 6 `.scm` tree-sitter query files vendored verbatim from aider `5dc9490bb35f` under Apache-2.0); Q2=A(revised) Tektos-internal, NO new `RepoMapPort` (ADR-023 envelope-first defer); Q3=C both per-file `tektos.repomap.indexed` MemoryPort writes + one per-run `tektos.repomap.snapshot` write; Q4=B linear-decay freshness `confidence = max(REPOMAP_MIN_CONFIDENCE, 1.0 - min(1.0, age_days/REPOMAP_FRESHNESS_WINDOW_DAYS))` locked in `policy.py`; Q5=C tiered tests (fast 500-file synthetic in `make stage1-gate`, env-gated 10k literal via `KOSMOS_STAGE_33_LARGE_CORPUS=1`, env-gated real CPython via `KOSMOS_STAGE_33_REAL_CORPUS=1`); Q6=A single composite ADR; locked constants `REPOMAP_PROVENANCE="aider-repomap"`, `REPOMAP_INDEXED_PREDICATE="tektos.repomap.indexed"`, `REPOMAP_SNAPSHOT_PREDICATE="tektos.repomap.snapshot"`, `REPOMAP_FRESHNESS_WINDOW_DAYS=30.0`, `REPOMAP_DEFAULT_MAP_TOKENS=1024`, `REPOMAP_CACHE_VERSION=4`, `REPOMAP_MIN_CONFIDENCE=0.01`; 7 new pip deps (`tree-sitter>=0.24`, `tree-sitter-language-pack>=1.13`, `networkx>=3.4`, `scipy>=1.14`, `grep-ast>=0.9`, `pygments>=2.18`, `diskcache>=5.6`); ADR-007/008/023/036/037 respected; DoD literal anchor `pytest plugins/tektos/tests/test_repomap.py::test_repomap_smoke_500_file_corpus_writes_queryable_via_memoryport` — 31 new tests (locked constants, freshness, tags, rank, render, indexer, smoke 500-file, +2 env-gated); 675 total green + 4 env-gated skips | **Ratified v25** | Stage 3.3 |

| ADR-039 | `ADR-039-stage-3-4-and-3-5-defer.md` | Defer Stage 3.4 (Bernstein Janitor spike) to Phase 4 and Stage 3.5 (Reflexion + Voyager port) to Phase 5 — both stages' DoDs literally reference substrate that other ratified ADRs defer or has not been built. §3.4 requires `SandboxProvider` + `WorktreeProvider` (absent from `ports/`) and Postgres TaskState schema (absent from tree) per ADR-004 §Evaluation Plan step 2; ADR-004 §Build-Order Placement literal already schedules the spike "immediately before Tektos Phase 4 begins." §3.5 DoD literal "Reflexion cycle logged in Langfuse" is blocked by ADR-025 (Langfuse deferred) + ADR-034 (`LangfuseTraceFeedAdapter` primary lands Stage 5). Docs-only ADR — no code churn, no port surface changes, no pip deps, no test churn. Build-Sequence §3.4 and §3.5 rewritten as defer-blocks preserving original scope text under "Original §… scope (deferred)" subsections. Phase 3 advances from Stage 3.3 (LANDED) directly to Stage 3.6 (OpenSpec). Amends ADR-004 (narrows spike-run timing) and ADR-025 (concretely locks the Reflexion-in-Langfuse DoD as blocked on Langfuse defer). No PORTING_LEDGER changes — Bernstein Janitor / `local-agentic-loop-sample` / Reflexion / Voyager entries remain `PLANNED` / `EVALUATING` exactly as before. | **Ratified v25** | Phase 3 (post-3.3) |
| ADR-040 | `ADR-040-tektos-openspec-parser-vendoring.md` | Tektos OpenSpec parser PATTERN-VENDORED (Stage 3.6) — Q1=A pattern-vendor at `plugins/tektos/openspec/{policy,models,parser,plan}.py` reimplementing the OpenSpec artifact parser + Plan producer in Python; upstream `Fission-AI/OpenSpec@2b3d368539132be6311e55db58899abbf5306b81` (MIT), no upstream source copied verbatim (algorithm ported from upstream `docs/concepts.md` + `docs/opsx.md` + `openspec/changes/fix-spec-parser-fidelity/` unified-reader design). Q2=Tektos-internal, NO new port surface (ADR-023 envelope-first defer, matching ADR-038 Q2 for repomap; `DataPort` — ADR-028 JSON-LD export — not reused because semantically wrong). Q3=C both per-artifact `tektos.openspec.artifact.parsed` MemoryPort writes + one per-change `tektos.openspec.plan.produced` write with confidence = completeness score. Q4 real fixture `plugins/tektos/tests/fixtures/openspec/add-dark-mode/{proposal.md, design.md, tasks.md, specs/ui/spec.md}` patterned after upstream OPSX walkthrough. Q5 fast-only single-tier tests inside `make stage1-gate` (<10ms per change dir). Q6 single composite ADR-040 + STATUS AMENDMENT on ADR-005 (which incorrectly asserted OpenSpec was already vendored). Locked constants `OPENSPEC_PROVENANCE="openspec-parser"`, `OPENSPEC_ARTIFACT_PREDICATE="tektos.openspec.artifact.parsed"`, `OPENSPEC_PLAN_PREDICATE="tektos.openspec.plan.produced"`, `OPENSPEC_UPSTREAM_COMMIT="2b3d368539132be6311e55db58899abbf5306b81"`, `OPENSPEC_UPSTREAM_LICENSE="MIT"`, `OPENSPEC_MIN_CONFIDENCE=0.05`, `OPENSPEC_REQUIRED_ARTIFACTS=frozenset({"proposal.md"})`. No new pip deps (stdlib-only parser). ADR-007 respected (AST-verified `test_openspec_subsystem_imports_no_other_plugins_adr_007`); ADR-008 respected (zero-trust guard passthrough test + all writes carry locked provenance + confidence ∈ [0.05, 1.0]); ADR-023 respected (envelope-first); ADR-028 respected (`DataPort` untouched); ADR-036/037/038 unaffected (Tektos subsystems orthogonal). DoD literal anchor `pytest plugins/tektos/tests/test_openspec.py::test_produce_plan_on_add_dark_mode_fixture_writes_queryable_events_build_sequence_3_6_dod` — 30 new tests (locked constants, completeness formula, fence mask, section iteration, artifact parsing, delta spec + fence + metadata + normative predicates, task parsing + fence-block filtering, directory walk + required-artifact enforcement, DoD literal, minimal-artifact case, ADR-007 AST guard, ADR-008 zero-trust passthrough); 705 total green + 4 env-gated skips | **Ratified v25** | Stage 3.6 |
| ADR-041 | `ADR-041-tektos-plan-renderer-and-first-plugin-descriptor.md` | Tektos plan renderer + first `PluginDescriptor` (Stage 3.7) — Q1=B pure-Python renderer at `plugins/tektos/renderer/{__init__,policy,models,project}.py` over the Stage 3.6 `Plan` dataclass (no upstream vendored; `spec-kit` stays `PLANNED · Source: TBD` per Q10 Option X defer); Q2=A no new port surface (`FrontendContractPort` reused, envelope-first per ADR-023); Q3=A `Panel(id="tektos.plan_approvals", slot=APPROVALS_QUEUE, priority=90, lazy_module="tektos/panels/PlanApprovalPanel")` — priority 90 sits below Praxis `praxis.approvals` at 100 (ADR-033 §Q1=C); Q4=A fail-closed `ChangeApprovalTier.HUMAN_REVIEW` for every card (ADR-037 default); Q5=A minimal MVP `PlanCard` (`change_id`, `rendered_summary`, task counts, delta ADDED/MODIFIED/REMOVED counts, `confidence`, `tier`, `approval_id`, `panel_id`, `to_delta()`); Q6=A MemoryPort event `tektos.plan.card_rendered` with `provenance="tektos_plan_renderer"` and `subject="<change_id>::<panel_id>"`; Q7=A new `plugins/tektos/plugin.py` housing `TektosPlugin` dataclass + `build_tektos_descriptor()` pure fn mirroring Phrouros bootstrap shape (fires ADR-036 Q4=B trigger); `ui_parity_status=IN_PROGRESS` at 3.7 → COMPLIANT at Stage 3.11; Q8=A single-tier fast tests in `make stage1-gate`; Q9=A new ADR (not amend ADR-005/040) + STATUS AMENDMENT on ADR-036 for Q4=B trigger firing; Q10=Option X defer ADR-005 Spec-Kit fate — `PORTING_LEDGER.md` `spec-kit` row remains `PLANNED` with ADR pointer updated to `ADR-005 · ADR-041`. Locked constants `TEKTOS_PLAN_RENDERER_PROVENANCE="tektos_plan_renderer"`, `TEKTOS_PLAN_CARD_PREDICATE="tektos.plan.card_rendered"`, `TEKTOS_PLAN_PROPOSING_DOMAIN="tektos"`, `TEKTOS_PLAN_APPROVAL_TIER=HUMAN_REVIEW`, `TEKTOS_PLAN_CARD_MIN_CONFIDENCE=0.05`, `TEKTOS_PLAN_APPROVAL_PANEL_ID="tektos.plan_approvals"`, `TEKTOS_PLAN_APPROVAL_PANEL_PRIORITY=90`, `TEKTOS_PLAN_APPROVAL_LAZY_MODULE="tektos/panels/PlanApprovalPanel"`. ADR-007 respected (AST-verified `test_renderer_and_plugin_import_no_other_plugins_adr_007`); ADR-008 respected (zero-trust guard passthrough + `_RejectingMemoryPort` propagation test); ADR-023 respected (envelope-first); ADR-031 respected (`Panel`/`PluginDescriptor` shapes unchanged); ADR-033 respected (Tektos priority 90 < Praxis priority 100); ADR-036 Q4=B trigger fired via STATUS AMENDMENT; ADR-037 respected (fail-closed HUMAN_REVIEW default). No new pip deps. DoD literal anchor `pytest plugins/tektos/tests/test_plan_renderer.py::test_produce_plan_renders_as_approvable_card_via_frontend_contract_port_build_sequence_3_7_dod` — 28 new tests (locked constants, clamp bounds, `project_plan_to_card` correctness + input validation, `render_and_gate_plan_card` order-of-operations + fail-closed tier + deterministic intention_id + zero-trust propagation, `TektosPlugin` start/stop idempotency + descriptor shape, Tektos-below-Praxis priority contract, ADR-007 AST guard, end-to-end DoD literal); 733 total green + 4 env-gated skips | **Ratified v25** | Stage 3.7 |
| ADR-042 | `ADR-042-tektos-pier-eval-harness.md` | Tektos Pier eval harness (Stage 3.8) — Q1=A vendor `datacurve-pier==0.3.0` from PyPI as dev-only optional dep (Apache-2.0, upstream `datacurve-ai/pier@fefa7475a32bb05271abdea378e8083c83eb5c35`), no source copied; Q2=A Docker-only `PierEnv` on Colossus, `modal`/`daytona` enum-only and gated by future ADR; Q3=A envelope-first, no new port surface — Pier invoked via subprocess through `pier run` CLI, trajectory JSON parsed back into `TrialVerdict`; Q4=A subsystem at `plugins/tektos/eval/{__init__,policy,models,harness}.py` + kernel runner `scripts/pier_eval.py` + `Makefile eval-gate` target; Q5=A executed-trajectory eval against Harbor-format tasks with Tektos-relevant agent; Q6=A one `tektos.eval.trial_completed` MemoryPort event per trial with `subject="<change_id?>::<task_name>::<trial_id>"`, `provenance="pier-eval-harness"`, `confidence=1.0` on PASS / `0.0` on FAIL or ERROR, `attributes` carrying `outcome`, `verifier_exit_code`, `trajectory_dir`, `pier_env`, `pier_version`, `pier_commit`, `peak_context_tokens`, `llm_call_count`, optional `change_id`; **Q7=B advisory only** (revised from Q7=A after ADR-007 mechanism review: no Tektos-only path to `ChangeApprovalProtocol.resolve()` exists — Q7=A would either violate ADR-007 by importing Praxis or double Stage 3.8 scope with a Praxis event bridge; deferred to future ADR-043 if needed); Q8=A two-tier tests, fast unit tier with fake `pier` CLI shim runs under `make stage1-gate`, real Pier tier env-gated by `KOSMOS_STAGE_38_REAL_PIER=1`; Q9=A new ADR-042 + STATUS AMENDMENT on ADR-006 (superseded — v20.2 framing did not survive v25 ports-plus-plugins architecture); Q10=A one committed Harbor fixture `plugins/tektos/eval/tasks/tektos-plan-execution-smoke/` (rename `greet_old` → `greet`, three verifier assertions). Locked constants `PIER_EVAL_PROVENANCE="pier-eval-harness"`, `PIER_TRIAL_PREDICATE="tektos.eval.trial_completed"`, `PIER_UPSTREAM_COMMIT="fefa7475a32bb05271abdea378e8083c83eb5c35"`, `PIER_UPSTREAM_LICENSE="Apache-2.0"`, `PIER_UPSTREAM_PACKAGE="datacurve-pier"`, `PIER_UPSTREAM_PYPI_VERSION="0.3.0"`, `PIER_DEFAULT_ENV="docker"`, `PIER_TIMEOUT_SEC=1800.0`, `PIER_MIN_CONFIDENCE=0.0`, `PIER_MAX_CONFIDENCE=1.0`. ADR-007 respected (AST-verified `test_eval_subsystem_imports_no_other_plugins_adr_007`); ADR-008 respected (zero-trust guard passthrough — provenance + bounded confidence on every write); ADR-023 respected (envelope-first, no port added); ADR-037 respected (`ApprovalGatewayPort` propose-only surface untouched); ADR-041 respected (advisory verdict does not mutate plan-card tier). New pip dep `datacurve-pier==0.3.0` (dev-only via `[project.optional-dependencies] eval`). pyproject `norecursedirs` excludes `plugins/tektos/eval/tasks` so Harbor verifier tests never run in the Kosmos suite. DoD literal anchor `pytest plugins/tektos/tests/test_pier_eval.py::test_tektos_plan_runs_through_pier_before_user_review_build_sequence_3_8_dod` — 14 new fast unit tests + 1 env-gated real-Pier tier (locked constants, confidence mapping bounds + type guard, `PierEnv.DOCKER` default, `TrialVerdict.to_attributes` JSON round-trip, `run_pier_trial` PASS/FAIL/non-Harbor-directory paths, `record_pier_verdict` locked-shape write + FAIL/ERROR zero-confidence, `run_and_record_trial` ERROR-verdict on `PierTrialFailure`, committed Harbor fixture shape, ADR-007 AST guard, DoD literal end-to-end); 747 total green + 5 env-gated skips | **Ratified v25** | Stage 3.8 |
| ADR-044 | `ADR-044-tektos-docling-document-ingestion.md` | Tektos docling document ingestion (Stage 3.10) — Q1=A PATTERN-VENDOR `docling==2.116.0` (MIT; upstream `docling-project/docling@ba8251e9cda84bab44cebe3b884119d3f50cb12a`), no source copied — docling is lazy-imported inside `resolve_default_converter_factory()`; Q2=A envelope-first, no new port surface, reuses existing `DataPort.export_canonical` per ADR-023 (defer pattern proven at ADR-038/040/041/042); Q3=A default `PIITier.INTERNAL` with caller override to SENSITIVE/RESTRICTED (RESTRICTED routes under `{root}/restricted/{record_type}/` per Spec §147); Q4=A `confidence=1.0` on success, `DoclingIngestFailure` raised on any failure with **no** envelope written; Q5=A two-tier tests (fast fake-shim tier under `make stage1-gate` + env-gated real docling via `KOSMOS_STAGE_310_REAL_DOCLING=1`); Q6=A extensions frozen to `DOCLING_SUPPORTED_EXTENSIONS = frozenset({".pdf", ".docx", ".html"})` matching DoD literal verbatim; Q7=A kernel runner `scripts/docling_ingest.py` + `Makefile ingest-doc` target; Q8=A new ADR-044 (was originally planned as ADR-043 but ADR-042 forward-references "candidate ADR-043" for Pier auto-approve — this ADR takes the next unused slot to preserve that reference), amends nothing; Q9=A DoD literal anchor test `test_pdf_docx_html_ingest_produces_structured_jsonld_via_dataport_build_sequence_3_10_dod`. Locked constants at `plugins/tektos/ingest/policy.py`: `DOCLING_INGEST_PROVENANCE="tektos-docling-ingest"`, `DOCLING_INGEST_RECORD_TYPE="tektos.ingest.document"`, `DOCLING_UPSTREAM_PACKAGE="docling"`, `DOCLING_UPSTREAM_PYPI_VERSION="2.116.0"`, `DOCLING_UPSTREAM_COMMIT="ba8251e9cda84bab44cebe3b884119d3f50cb12a"`, `DOCLING_UPSTREAM_LICENSE="MIT"`, `DOCLING_DEFAULT_PII_TIER=PIITier.INTERNAL`, `DOCLING_SUCCESS_CONFIDENCE=1.0`, `DOCLING_MIN_CONFIDENCE=0.0`, `DOCLING_MAX_CONFIDENCE=1.0`, `DOCLING_SUPPORTED_EXTENSIONS=frozenset({".pdf", ".docx", ".html"})`. New pip dep group `[project.optional-dependencies] ingest = ["docling==2.116.0"]`. Setuptools package list gains `plugins.tektos.ingest`. ADR-007 respected (AST-verified `test_docling_ingest_imports_no_other_plugins_adr_007`); ADR-008 respected (zero-trust guard passthrough for MemoryPort surfaces; ingest itself writes only through DataPort); ADR-023 respected (envelope-first, no port added); ADR-028 respected (`DataPort` untouched, JSON-LD envelope + canonical hash + signature intact); ADR-042 preserved (forward-reference to "candidate ADR-043" untouched). DoD literal anchor `pytest plugins/tektos/tests/test_docling_ingest.py::test_pdf_docx_html_ingest_produces_structured_jsonld_via_dataport_build_sequence_3_10_dod` — 26 new fast unit tests + 1 env-gated real-docling tier; 791 total green + 7 env-gated skips | **Ratified v25** | Stage 3.10 |
| ADR-045 | `ADR-045-tektos-ui-htmx-dashboard.md` | Tektos UI HTMX dashboard (Stage 3.11) — Q1=C web dashboard via FastAPI + vendored HTMX 2.0.4 (`plugins/tektos/ui/htmx.min.js` 50917 B, sha256 `e209dda5c8235479f3166defc7750e1dbcd5a5c1808b7792fc2e6733768fb447`, upstream `bigskysoftware/htmx@b82cf843e47e575dd8c2ad8fee547d8e2c3bb87f`, license `0BSD`), 127.0.0.1:8765, 6-route surface, no auth; fast unit tier uses FastAPI TestClient (Q1d=A) so DoD literal never binds a real port. Q2=A reuse ADR-041 `Panel(id="tektos.plan_approvals", slot=APPROVALS_QUEUE)` — parity flips COMPLIANT via a new `Route(path="/tektos", ...)` on the Tektos descriptor so `_derive_parity(routes ∧ panels)` returns COMPLIANT. Q3=A `NopExecutor` implements `ExecutorPort` Protocol. Q4=A pure-stdlib `difflib.unified_diff` for /diff. Q5=A three per-transition MemoryPort events with locked shape `subject="<change_id>::<approval_id>"`, `provenance="tektos_ui"`, `confidence=1.0`: predicates `tektos.plan.approved` / `tektos.plan.executed` / `tektos.plan.diff_rendered`. Q6=A all Tektos plans stay HUMAN_REVIEW at 3.11. Q7=B two-tier tests: fast unit tier default + env-gated interactive tier `KOSMOS_STAGE_311_INTERACTIVE=1` spawning uvicorn via `scripts/tektos_ui.py`. Q8=C new ADR-045 + STATUS AMENDMENT on ADR-041 (ui_parity_status IN_PROGRESS → COMPLIANT). Q9=A reserves ADR-043 slot for deferred Pier auto-approve. Q10=A DoD literal anchor `test_plan_approve_execute_diff_flow_visible_in_kernel_dashboard_build_sequence_3_11_dod`. Q_res_1=B port-level `ApprovalResolverPort.list_pending(*, proposing_domain=None)`; `PraxisApprovalResolverAdapter` wraps `KernelChangeApprovalAdapter` and applies filter client-side. Q_res_2=B UI approvals set `resolved_by="tektos_ui"`. Promotion=A `ApprovalRecord`+`ApprovalStatus` promoted to `ports/approval.py` (backward-compat re-export from `plugins.praxis.apex.models`). Locked constants at `plugins/tektos/ui/policy.py`: `TEKTOS_UI_PROVENANCE="tektos_ui"`, `TEKTOS_UI_RESOLVED_BY="tektos_ui"`, `TEKTOS_UI_HOST="127.0.0.1"`, `TEKTOS_UI_PORT=8765`, `TEKTOS_UI_PLAN_APPROVED_PREDICATE="tektos.plan.approved"`, `TEKTOS_UI_PLAN_EXECUTED_PREDICATE="tektos.plan.executed"`, `TEKTOS_UI_PLAN_DIFF_RENDERED_PREDICATE="tektos.plan.diff_rendered"`, `TEKTOS_UI_SUCCESS_CONFIDENCE=1.0`, `TEKTOS_UI_HTMX_VERSION="2.0.4"`, `TEKTOS_UI_HTMX_UPSTREAM_COMMIT="b82cf843e47e575dd8c2ad8fee547d8e2c3bb87f"`, `TEKTOS_UI_HTMX_UPSTREAM_LICENSE="0BSD"`, `TEKTOS_UI_HTMX_SHA256="e209dda5c8235479f3166defc7750e1dbcd5a5c1808b7792fc2e6733768fb447"`, `TEKTOS_UI_PROPOSING_DOMAIN="tektos"`, `TEKTOS_UI_ROUTE_PATH="/tektos"`, `TEKTOS_UI_ROUTE_LABEL="Tektos"`, `TEKTOS_UI_ROUTE_ICON="📐"`, `TEKTOS_UI_ROUTE_LAZY_MODULE="tektos/pages/DashboardPage"`, `TEKTOS_UI_INDEX_PATH="/"`, `TEKTOS_UI_PLAN_DETAIL_PATH="/plan/{approval_id}"`, `TEKTOS_UI_PLAN_APPROVE_PATH="/plan/{approval_id}/approve"`, `TEKTOS_UI_PLAN_EXECUTE_PATH="/plan/{approval_id}/execute"`, `TEKTOS_UI_PLAN_DIFF_PATH="/plan/{approval_id}/diff"`, `TEKTOS_UI_HEALTHZ_PATH="/healthz"`, `TEKTOS_UI_HTMX_JS_PATH="/htmx.min.js"`. New pip deps `[project.optional-dependencies] ui = ["fastapi>=0.115", "uvicorn>=0.32", "httpx>=0.27"]`. Setuptools package list gains `plugins.tektos.ui`, `adapters.approval_resolver`, `adapters.approval_resolver.praxis`; package-data ships `plugins.tektos.ui/htmx.min.js`. ADR-007 respected; ADR-008 respected; ADR-023 respected; ADR-031 respected (`_derive_parity(routes ∧ panels)` → COMPLIANT); ADR-033 respected; ADR-041 amended. DoD literal anchor `pytest plugins/tektos/tests/test_tektos_ui.py::test_plan_approve_execute_diff_flow_visible_in_kernel_dashboard_build_sequence_3_11_dod` — 24 new fast unit tests + 1 env-gated interactive tier + 5 adapter contract tests + 3 additions to `test_plan_renderer.py`; 815 total green + 8 env-gated skips | **Ratified v25** | Stage 3.11 |
| ADR-046 | `ADR-046-stage-3-exit-gate-tektos-end-to-end-refactor.md` | Stage-3 exit gate (Stage 3.12) — Tektos refactors one real Kosmos file end-to-end via the full 3.1→3.2→3.3→3.6→3.7→3.11 pipeline (3.8 Pier + 3.10 docling deferred to Phase 4). Q1=A refactor target `plugins/tektos/ui/templates.py` (extract-method: `_escape_record_fields(record) -> tuple[str,str,str,str]` helper unifies duplicated `html.escape(str(...))` block across `render_pending_row` + `render_plan_detail`). Q2=A extract-method (mechanical, low-risk, obvious). Q3=B pipeline depth = fire real 3.1/3.2/3.3/3.6/3.7/3.11 (skip 3.8/3.10 per ADR-039-adjacent scope). Q3.1=C two-tier LLM: fast tier uses Interp-2 (human-authored deterministic instruction), interactive env-gated `KOSMOS_STAGE_312_INTERACTIVE=1` tier uses Interp-1 real Ollama. Q4=A `bandit>=1.7` in `[project.optional-dependencies] dev` + `[tool.bandit]` config (`exclude_dirs=[".venv","build","dist","__pycache__"]`, `skips=["B101"]`). Q5=A new `scripts/stage3_gate.py` mirroring `scripts/stage1_gate.py` shape (5 criteria: BUILD_LOG entry present, refactor commit SHA discoverable by `git log --grep="Stage 3.12 · Tektos refactor · extract-method"`, ruff clean on refactor target, bandit clean, full pytest green). Q6=A two-commit shape: commit 1 (Tektos-authored `Tektos <tektos@kosmos.local>` — refactor only, subject literal `Stage 3.12 · Tektos refactor · extract-method`) + commit 2 (rmholston420 — DoD test + gate + ADR + fanout + logs), tag `stage-3-12-complete` on commit 2. Q7=A fake Pier (not exercised in pipeline). Q8=A `TestClient` only for 3.11 UI touchpoint (no real uvicorn). Q9=A single composite ADR-046. Q10=A DoD literal test name `test_tektos_refactors_real_kosmos_file_end_to_end_passes_ruff_bandit_pytest_build_sequence_3_12_dod`. New optional pip dep `bandit>=1.7` (dev only). Fixture change committed at `plugins/tektos/tests/fixtures/openspec/refactor-tektos-ui-templates-extract-escape-helpers/{proposal.md, tasks.md, specs/tektos-ui-templates/spec.md}`. ADR-007 respected (test does not import cross-plugin); ADR-008 respected (real MemoryPort writes carry provenance + confidence via 3.3/3.6/3.7 subsystems); ADR-023 respected (envelope-first, no port added); ADR-033/037 respected (APEX + `TektosToolCallPending` behavior unchanged); ADR-036/037/038/040/041/045 respected (all six pipeline stages fired unmodified). DoD literal anchor `pytest plugins/tektos/tests/test_stage_3_12_exit_gate.py::test_tektos_refactors_real_kosmos_file_end_to_end_passes_ruff_bandit_pytest_build_sequence_3_12_dod` — 5 fast tests + 1 env-gated interactive tier; 825 total green + 9 env-gated skips | **Ratified v25** | Stage 3.12 |
| ADR-047 | `ADR-047-stage-4-2-corpora-hybrid-tier.md` | Stage 4.2 Graphiti tuning · real backends + Hybrid-tier corpora. Q1=corpora location `adapters/memory/dozerdb/corpora/` (rejected `plugins/gnosis/` — Gnosis lands Stage 4.4 per ADR-002 + Build-Sequence §4.4). Q2=Hybrid tier: fast (always-green via `InMemoryTemporalIndex`, 34 tests, DoD-asserted) + live (env-gated `KOSMOS_STAGE_42_LIVE=1` against Compose DozerDB + local Ollama, 3 tests, opportunistic metrics). Q3=LLM/embedder path Colossus-local Ollama (`qwen3-coder` LLM + `nomic-embed-text` embedder at `$OLLAMA_URL` default `http://localhost:11434/v1`); Graphiti instantiated with `OpenAIGenericClient` + `OpenAIEmbedder` + `OpenAIRerankerClient` all pointed at Ollama (rejected hosted OpenAI/Anthropic default — violates local-first custom instructions). Q4=three corpora `synthetic-lifeline` (10 R.M. Holston lifeline facts 1972–2026, 4 queries), `humanities-cidoc-sample` (5 CIDOC-CRM Buddhist facts, 2 queries), `rigpa-export` (20-event fixture at `adapters/memory/dozerdb/corpora/fixtures/rigpa_sample.jsonl`, overridable via `KOSMOS_RIGPA_EXPORT_PATH`, 3 queries). Live-tier first run 2026-07-30: 37 passed / 1 warning in 137.29 s on Colossus — amortized ~3.9 s/fact (Graphiti + Ollama first-run index build + semantic search folded in). Fast-tier record_event/query_temporal < 1 ms (in-memory). Real backends landed: `DozerDbGraphBackend` (Bolt driver against `graphstack/dozerdb:5.26.27`), `GraphitiTemporalIndex` (Graphiti + Ollama), `AmgV02Policy` (`agent-memory-guard==0.2.2` MemoryGuard wrapper). Compose service at `ops/compose/memory.yml` (Bolt 7687, heap ≤ 4 GiB, page-cache ≤ 2 GiB). ADR-007 respected (corpora are an internal adapter package, not a plugin); ADR-008 respected (every corpus fact carries provenance + confidence); ADR-027 unchanged (`graphiti-core>=0.5` pin preserved, local-Ollama wiring done at construction time). New Compose surface + optional env-gated tier only — no new pip deps. DoD anchor `pytest adapters/memory/dozerdb/corpora/test_corpora_contract.py` — 34 fast tests + 3 env-gated live tests; whole-repo test count updated per commit; `docs/PORT_CONTRACTS.md` created with MemoryPort surface + measured metrics; tag `stage-4-2-complete` | **Ratified v25** | Stage 4.2 |
| ADR-048 | `ADR-048-stage-4-3-amg-v03-adoption.md` | Stage 4.3 · Agent Memory Guard v0.3.0 adoption + `Policy.tiered()` default. Bumped `pyproject.toml` `agent-memory-guard==0.2.2` → `==0.3.0` (v0.3.0 released upstream 2026-06-10; on PyPI; API is a strict superset of v0.2.2). Concrete wrapper renamed `AmgV02Policy` → `AmgGuardPolicy` in new module `adapters/memory/dozerdb/amg_policy.py`; `AmgV02Policy` retained as backwards-compat alias through Stage 5; old `amg_v02_policy.py` reduced to a re-export shim. Default preset switched from `Policy.strict()` to v0.3.0's `Policy.tiered()` (new default memory-class taxonomy — session / durable / promoted; aligns with the Kosmos memory-lifecycle model exercised by Stage 4.2 corpora); `Policy.strict()` remains selectable via `AmgGuardPolicy(policy_preset="strict")`. v0.3.0 write kwargs `source_class`/`receipt_uri`/`memory_class` (or `cls`)/`task_id`/`source` are opt-in via payload keys — extracted from `evaluate(payload)`, forwarded to `MemoryGuard.write(...)`, and stripped from the JSON body so routing fields never pollute the semantic write. MCP server / CLI scanner / GitHub Action / LlamaIndex + CrewAI integrations / Prometheus exporter / ML injection detector explicitly **NOT adopted** at 4.3 — each is its own future ADR when needed. Zero-trust fail-safe preserved (init/write/snapshot failure → `AmgVerdict(decision="block")`). `AmgPolicy` Protocol shape unchanged; `DozerDbMemoryAdapter` DI seams unchanged. Contract test renamed `test_amg_v02_policy_contract.py` → `test_amg_policy_contract.py`; 20 fast + 2 env-gated live; DozerDB adapter fast-tier 130 passed / 7 skipped. No new pip deps. ADR-007/008/023/027/047 respected. DoD anchor `pytest adapters/memory/dozerdb/test_amg_policy_contract.py`; tag `stage-4-3-complete` | **Ratified v25** | Stage 4.3 |
| ADR-049 | `ADR-049-stage-4-4-superpowers-kb-adapter-corpus.md` | Stage 4.4 · Superpowers KB as MemoryPort adapter corpus (full-body, MIT). Q1=adapter corpus at `adapters/memory/dozerdb/corpora/superpowers/` (rejected `plugins/gnosis/` — Gnosis lands Phase 3; rejected top-level `kbs/superpowers/` bypass of Stage 4.2 contract). Q2=pinned SHA `44c9b2d6e889982ac18c27d05a19fefe335194e1` + workspace-local re-ingest CLI `scripts/ingest_superpowers.py` (rejected scheduled `main` re-check + one-shot no-CLI). Q3=adapter now, relocates to `plugins/gnosis/humanities/personal_kb/` at Phase 3 (rejected premature-plugin-skeleton + never-move; loader shape stable across the move). Q4=temporal + typed-link retrieval via new `CorpusEdge` records (inline Markdown `[text](path)` sibling links; 9 edges at 4.4); VectorPort surface deliberately NOT opened (rejected temporal-only + temporal+vector). Q5=per-file granularity, one MemoryPort record per `skills/*/*.md` at pinned SHA — 38 records across 14 skill directories (rejected per-skill + per-section). Q6=full-body Markdown ingest with `body`+`source_commit`+`license="MIT"`+`upstream_url`+typed `references` in `attributes` (rejected pointer-only). Reconciles ADR-008 (Tektos-UX "do not vendor Superpowers code") with ADR-002 + ADR-016 (Personal-KB substrate under Gnosis) — Superpowers content enters as MemoryPort **data**, not plugin code; both rules coexist. New: `adapters/memory/dozerdb/corpora/superpowers/{__init__.py,superpowers.py,fixtures/superpowers.jsonl}` (~310 KB fixture) + `scripts/ingest_superpowers.py`. Extended: `models.py` adds `CorpusEdge` (frozen slots) + optional `Corpus.edges: tuple[CorpusEdge,...]` (defaults `()`, backward-compatible with Stage 4.2 corpora, construction-time invariants enforce src/dst resolvability + kind non-empty). Corpora `__init__.py` exports `SUPERPOWERS_CORPUS`/`CorpusEdge`/`load_superpowers_corpus`; `ALL_CORPORA` grows to four. Tests: 7 new fast (cardinality ≥30 across ≥10 subjects, provenance triple, typed-edge resolvability, env override, missing-attr rejection, fixture-committed) + ADR-007 AST scan upgraded to `rglob("*.py")` for subpackage coverage. DozerDB adapter suite 142 passed / 8 skipped (up from 130/7 at Stage 4.3). No new pip deps, no runtime network fetch. ADR-007/008/016/047/048 respected. DoD anchor `pytest adapters/memory/dozerdb/`; tag `stage-4-4-complete` | **Ratified v25** | Stage 4.4 |

| ADR-050 | `ADR-050-stage-4-5-humanities-bilara-adapter-corpus.md` | Stage 4.5 · SuttaCentral Bilara humanities corpus as MemoryPort adapter corpus (CC0). Q1=Bilara (CC0) — pivoted from 84000 CC-BY-NC-4.0 to eliminate NC downstream propagation; Bilara directory mirror between `translation/<lang>/<translator>/**` and `root/<lang>/<edition>/**` is literally CIDOC-CRM `P73_has_translation` (rejected 84000 alone + rejected both). Q2=pinned SHA `3c93d1cea80fdebcefb777c8724c35bd971f360a` + workspace-local re-ingest CLI `scripts/ingest_humanities.py --sha <SHA> [--via gh|checkout]` (rejected `published`-branch tracking + one-shot no-CLI). Q3=adapter now at `adapters/memory/dozerdb/corpora/humanities_bilara/`, relocates to `plugins/gnosis/humanities/canonical_kb/` at Phase 3 (rejected premature-plugin + never-move; loader shape stable across move). Q4=temporal + typed CIDOC-CRM link retrieval via `CorpusEdge` with `P73_is_translation_of` (70) + `P94_was_created_by` (70) = 140 edges at 4.5 landing; VectorPort surface NOT opened; **untyped `references` kind explicitly rejected** — CIDOC-CRM property URIs required for external KG interop (rejected temporal-only + temporal+vector). Q5=per-file granularity: one record per `translation/<lang>/<translator>/**/*.json` + one per mirrored root JSON + one per referenced translator `E21_Person` actor from `_author.json` (rejected per-publication roll-up + per-segment splitting + omitting actor records — latter breaks Corpus resolvability invariant). Q6=full-body segment-keyed JSON ingest with `body`+`segment_count`+`source_commit`+`license` (`CC0-1.0` translations / `public-domain` root)+`upstream_url`+translator/publication metadata+typed `references`; Stage 4.5 slice = Sujato's English translations of scpub7 Dhammapada + scpub19 Khuddakapatha + scpub86 Cariyapitaka mirrored by Mahasangiti Pali root under `root/pli/ms/sutta/kn/{dhp,kp,cp}/` = 141 records (70 translation + 70 root + 1 actor), 140 CIDOC-CRM edges, ~392 KB fixture (rejected pointer-only). Reconciles ADR-002 + ADR-016 (Humanities substrate under Gnosis) with Stage 4.2 corpora contract (ADR-047) — Bilara content enters as MemoryPort **data**, not plugin code. Stage 4.2 `humanities_cidoc_sample` corpus stays as fast-tier CIDOC-CRM invariants probe — NOT superseded. New: `adapters/memory/dozerdb/corpora/humanities_bilara/{__init__.py,humanities_bilara.py,fixtures/humanities_bilara.jsonl}` + `scripts/ingest_humanities.py`. Extended: corpora `__init__.py` exports `HUMANITIES_BILARA_CORPUS` + `load_humanities_bilara_corpus`; `ALL_CORPORA` grows to five. Loader validates three subject namespaces (`bilara/actor/`, `bilara/root/`, `bilara/translation/`) with per-namespace required-attribute lists; unknown namespaces rejected. Tests: 7 new fast (cardinality-by-namespace 1/70/70, provenance triple + CIDOC-CRM class labels `E33_Linguistic_Object`/`E21_Person`, typed-edge kind census {P73_is_translation_of:70, P94_was_created_by:70} + resolvability, root/translation bijection at `bilara_uid`, env override `KOSMOS_HUMANITIES_BILARA_PATH`, missing-attribute + unknown-namespace rejection, fixture-committed check). DozerDB adapter suite 155 passed / 9 skipped (up from 142/8 at Stage 4.4). No new pip deps, no runtime network fetch. ADR-007/008/016/047/049 respected. DoD anchor `pytest adapters/memory/dozerdb/`; tag `stage-4-5-complete` | **Ratified v25** | Stage 4.5 |

| ADR-051 | `ADR-051-stage-4-6-exit-gate-gnosis-surrogate.md` | Stage 4.6 · exit gate materialized as adapter-side FastAPI surrogate at `adapters/memory/dozerdb/gate/` — reads the five landed corpora as-is; Phase-3 Gnosis will wrap or replace this surface. Q1=adapter-side surrogate at `adapters/memory/dozerdb/gate/` (rejected `plugins/gnosis/ui/` — no Gnosis code exists, stub plugin would need deletion at Phase 3). Q2=federated across all five landed corpora (`synthetic-lifeline`, `humanities-cidoc-sample`, `rigpa-export`, `superpowers`, `humanities-bilara`) — every corpus addressable at `/corpus/{name}` (rejected single-corpus scope). Q3=fast tier is DoD anchor via FastAPI `TestClient`; live tier bound to `127.0.0.1:8746` behind `KOSMOS_STAGE_46_LIVE=1` (rejected live tier as DoD anchor — CI would need port binding). Q4=FastAPI application factory `build_stage_46_gate_app(*, corpora)` mirroring Tektos UI (Stage 3.11) shape — six locked routes: `/` (dashboard), `/corpus/{name}` (detail), `/corpus/{name}/provenance/{event_id}` (chain), `/corpus/{name}/query` (temporal), `/corpus/{name}/traverse/{event_id}` (typed edges), `/healthz` — pure-Python HTML fragment templates with `html.escape` on every user string, no jinja/htmx (rejected plain route module — misses stateless factory pattern). Q5=one temporal query + one CIDOC-CRM traversal, both required: (a) 70 Bilara translation records surfaced with `provenance`+`as_of`+`confidence`; (b) outbound edges from any Bilara translation resolve to exactly {`P73_is_translation_of`, `P94_was_created_by`}. Q6=default confidence `1.0` for corpus-sourced facts at 4.6 (rejected per-corpus tunable defaults — premature until Stage 5 Graphiti derivations exist). Ports: gate on 8746 (distinct from Tektos UI 8765). New: `adapters/memory/dozerdb/gate/{__init__.py,policy.py,models.py,traversal.py,templates.py,server.py,test_stage_46_gate.py}`. No new pip dep (FastAPI already vendored from Stage 3.11 Tektos UI — no PORTING_LEDGER change). No plugin imports (ADR-007 AST guard test enforces `gate/*.py` never imports `plugins.*`). Zero-trust preserved — gate is read-only; `STAGE_46_PROVENANCE="stage_46_gate"` reserved for any future write path. Tests: 19 new fast + 1 env-gated live; DozerDB adapter tier 174 passed / 10 skipped (up from 155/9 at Stage 4.5); whole-repo fast tier 957 passed / 19 skipped. Reconciles ADR-002 + ADR-016 (Gnosis Phase 3 substrate) with the Stage 4.6 DoD verb ("answers a temporal question with full provenance chain") — the DoD reads at the retrieval surface, not at a plugin, so materializing the surface at the adapter layer is sufficient. DoD anchor `pytest adapters/memory/dozerdb/gate/`; tag `stage-4-6-complete` | **Ratified v25** | Stage 4.6 |

| ADR-052 | `ADR-052-stage-6-1-zetesis-skeleton.md` | Stage 6.1 · Zetesis plugin skeleton — kernel-plugin at `plugins/zetesis/` with dataclass-plus-async-start-stop shape mirroring Praxis/Phrouros/Tektos. Q1=A amend ADR-015 with status-amendment block preserving original text; author this ADR for concrete 6.1 lock-in (rejected supersede-ADR-015 — Stage 5 deferred, not cancelled). Q2=A `build_zetesis_descriptor()` returns descriptor with **zero panels, zero routes, empty design tokens** — kernel discovers Zetesis but nothing renders yet; UI surface lands Stage 6.3/6.4 (rejected `PanelSlot.RESEARCH_FEED` addition — requires `ports/frontend_contract.py` amendment + separate ADR, scope creep against DoD literal 'Plugin loads'). Q3=A skeleton is inner-loop-agnostic; 10 required business ports held as constructor deps but **called zero times** at 6.1; no `ResearchInnerLoop` Protocol seam; ADR-010 head-to-head remains pristine pre-Phase-6.2 (rejected abstract-Protocol-seam — risks pre-empting ADR-010's decision surface). Enforced by `test_start_touches_no_business_port` binding all 10 ports to `_UntouchablePort` sentinels that raise `AssertionError` on any attribute access. Q4=confirmed locked MemoryPort constants at 6.1 even though first write lands Stage 6.3: `ZETESIS_MEMORY_PROVENANCE="zetesis_research"`, `ZETESIS_MEMORY_PREDICATE="zetesis.research.completed"`, `ZETESIS_MEMORY_DEFAULT_CONFIDENCE=0.75` (mirrors ADR-036 Tektos pre-Reflexion default; sits in `(0,1]` per ADR-008 zero-trust guard). Q5=C real `ZetesisPlugin` **is** the `zetesis-stub` from spec §191 + Build-Sequence §1.6; no separate stub package; Phase-1 stub-role debt closes at 6.1 landing (rejected A: build separate `plugins/zetesis_stub/` — creates code spec says will be deleted at Phase 6; rejected B: KNOWN_ISSUES.md deferral — leaves obligation unresolved). Q6=A single composite ADR covers Q1–Q7 (they're load-bearing on each other; splitting would fragment the lock-in trail). Q7=B-plus 10 required (non-None) + 1 optional port slot — corrects Build-Sequence §6.1's stale 4-port list vs. spec §95 (SearchPort omission from ADR-021) + spec §172/§191 implicit ResourcePort requirement from Q5=C stub-role. Required: FrontendContractPort, LLMPort, MemoryPort, VectorPort, DataPort, SearchPort (Q7 correction), EventBusPort (Q7 addition), ResourcePort (Q7 addition; required by stub-role obligation), NotificationPort (Q7 addition; algedonic path for grounding-failure escalation per spec §46), ObservabilityPort (Q7 addition; trace + metrics for every 6.3+ inner-loop call). Optional (may be `None` at 6.1): SecretsPort (external-service credentials, wired when non-local SearchPort backend or paywalled data source is added). New files: `plugins/zetesis/{__init__.py, plugin.py, tests/__init__.py, tests/test_zetesis_plugin.py}`. No new pip deps; no PORTING_LEDGER change (skeleton is purpose-written, no OSS port). ADR-007 respected (AST scan of `plugins/zetesis/**/*.py` finds zero imports of `plugins.praxis`/`plugins.phrouros`/`plugins.tektos`); ADR-008 respected (write constants pinned in `(0,1]` for Stage 6.3+ writes); ADR-010 preserved (zero `LLMPort` calls at 6.1); ADR-015 amended (Stage-5 deferred); ADR-021 preserved (SearchPort promoted to required); ADR-029 preserved (ResourcePort priority queue wired for Stage 6.3+ inference). Test surface: 29 fast contract tests (locked constants x8, descriptor shape x5, construction/lifecycle/idempotency x11, ADR-007 AST guard x1, port-surface holds x2, `_UntouchablePort` proof x1, `SecretsPort` optional-slot x1). Whole-repo fast tier: 986 / 19 (up from 957 / 19 at Stage 4.6, delta +29 = new Zetesis tier exactly). DoD anchor `pytest plugins/zetesis/`; tag `stage-6-1-complete` | **Ratified v25** | Stage 6.1 |

| ADR-056 | `ADR-056-stage-6-3-proper-zetesis-kernel-wiring.md` | Stage 6.3 (proper) · Zetesis kernel wiring — the outer §6.3 verb (DoD: "Zetesis produces a multi-source research report with citations") enabled by ADR-055's substrate ratification. Locks user's three scoping decisions (2026-07-30 22:02 EDT): (Q1=B) lift `run_odr_trial` and its 12 supporting modules from `ops/benchmarks/adr_010/harness/` to `plugins/zetesis/research/` and add a harness backward-compat shim (dependency-inverted — Zetesis owns its own inner loop; benchmarks continue to work via `plugins/zetesis/research.odr` re-exports); (Q2=B) wire all 10 required business ports this stage, not just `LLMPort` — `_UntouchablePort` sentinel deleted from `plugins/zetesis/plugin.py`, `ZetesisPlugin.__init__` accepts real adapters, 9 stub adapters land under `plugins/zetesis/adapters/`, 10 fast-tier port-wiring contract tests added; (Q3=A) reuse the ADR-010 F1–F6 fixture (Neo4j Community vs. DozerDB) with 5.33/6 as the regression floor — 1 Colossus trial through `ZetesisPlugin.research()` that must land ≥ 4.83 (0.5 tolerance around Stage 6.3.9 baseline, variance ≈ 0.056). ADR-052 §Q3=A skeleton discharged; §Q4 MemoryPort constants inherited; §Q7=B-plus port surface bound to real adapters. Sub-slice order (5 sub-slices, one commit + BUILD_LOG entry each): (1) harness lift + test co-move; (2) port-wiring skeleton + stub adapters + 10 contract tests; (3) `ZetesisPlugin.research()` method wiring all 10 ports around the lifted inner loop; (4) Colossus DoD trial + rating; (5) lock-in + tag `stage-6-3-complete`. Zero PORTING_LEDGER change (ODR stays VENDORED; only Kosmos-side location changes). ADR-007 respected (no plugin-to-plugin coupling). ADR-008 respected (all MemoryPort writes carry provenance + confidence). AREX-Turbo re-comparison remains deferred per KNOWN_ISSUES.md. Lock-in condition: this ADR ratified, all 5 sub-slices landed, DoD trial ≥ 4.83 on ADR-010 fixture, tag `stage-6-3-complete` pushed | **Ratified v25** | Stage 6.3 (proper) |
| ADR-055 | `ADR-055-stage-6-4-odr-tuned-ratification.md` | Stage 6.4 · ADR-010 substrate-tuning arc closure — ODR-post-6.3.9 (commit `05366ac`, tag `stage-6-3-9-complete`, agent-rated mean 5.33 / 6 on 3 Colossus trials at Stage 6.3.9) ratified as Zetesis's research inner loop for Stage 6.3 (proper) kernel wiring. Amends ADR-010: extends the Stage 6.2 winner-lock (ODR 3/3 vs AREX 0/3, aggregate 16.7% blind-rated F1–F6) with the Stage 6.3.x tuning arc result — ODR raised from 16.7% (Stage 6.2 baseline) to 89% (5.33 / 6 at Stage 6.3.9) across sub-stages 6.3.1 → 6.3.9. AREX-Turbo re-comparison against the tuned ODR is **deferred** (not cancelled) — filed to `KNOWN_ISSUES.md` as a non-blocking follow-up; the Stage 6.2 rejection reason (AREX completion 0/3 on context-ceiling exhaustion) remains dispositive because structural-finalize (ADR-053) does not address context-ceiling. AREX contender stays wired in the harness; vendored `BAAI/AREX-Turbo` inference bundle stays at `vendor/adr_010/arex_inference/`; structural-finalize parity work for AREX is on hold, not rejected. Zero PORTING_LEDGER change (ODR already VENDORED, AREX already REJECTED for Stage 6.2). ADR-007 respected (no plugin touch). ADR-008 respected (no new MemoryPort write). ADR-052 consequence: Zetesis `LLMPort` slot can bind to real substrate at Stage 6.3 (proper) instead of `_UntouchablePort` sentinel. ADR-054 consequence: 5.33 / 6 becomes the Stage 6.3 (proper) wiring regression floor on the same fixture. Scoping/ratification stage — no new Colossus trials, no PORTING_LEDGER change, no port surface change. Lock-in condition: this ADR ratified, ADR-010 amended, KNOWN_ISSUES entry filed, tag `stage-6-4-complete` | **Ratified v25** | Stage 6.4 |
| ADR-054 | `ADR-054-stage-6-3-9-finalize-polish.md` | Stage 6.3.9 · ADR-010 ODR finalize polish. Amends ADR-053 (does not supersede): (Q1) adds a new rule 6 to `build_structural_finalize_prompt` requiring verbatim preservation of rationale clauses introduced by "chosen to / to avoid / because / so that / in order to / specifically to" — F4 (DozerDB GPLv3 vs ONgDB AGPLv3 network-copyleft rationale) was silently compressed away in every 6.3.8 trial; (Q2) adds `_NUMERIC_ONLY_LABEL` regex + `_short_form_from_url` + `_normalize_source_label` to rewrite numeric-only citation labels (e.g. `"(2)"`) to a URL-derived domain short-form (`github.com/DozerDB`) in the sources block — renderer-side, audit trail preserved; (Q3) `rubric_critique` (shim 6) + `cove` (shim 7) silent no-ops (`no_fenced_output` / `insufficient_claims claims_found=0` on every 6.3.8 trial) diagnosed as pre-existing parser/prompt mismatch predating 6.3.7 — deferred to KNOWN_ISSUES, both shims remain enabled (structural finalize covers the gap). Zero PORTING_LEDGER change; ADR-053 structural contract (schema + allow-list gate + deterministic render + best-effort fallback + shim ordering) preserved. Whole-repo fast tier: 1199 → 1204 passed (+5), 19 skipped unchanged. Lock-in outcome (2026-07-30 21:47 EDT): Colossus 3-trial 6.3.9 agent-rated mean **5.33 / 6** (5.5 / 5.5 / 5.0), variance ≈ 0.056; Q1 verified (F4 rationale preserved 3/3) and Q2 verified (zero numeric-only labels 3/3); F6 "only if community demands" tail omitted 3/3 (stable ceiling, not a 6.3.9 regression); lock-in floor revised to **5.33** (initial target was 5.67 against user-rated 6.3.8 baseline; rater drift on F6 tail-check accounts for the delta) — 5.33 becomes ODR's rated baseline for Stage 6.3 (proper) Zetesis wiring (superseded by ADR-055 — the Stage 6.4 head-to-head re-comparison is deferred; ADR-055 ratifies ODR-post-6.3.9 as Zetesis's research inner loop) | **Ratified v25** | Stage 6.3.9 |
| ADR-053 | `ADR-053-adr-010-odr-structural-finalize.md` | Stage 6.3.8 · ADR-010 ODR structural-finalize shim — JSON-schema-constrained finalize turn via Ollama `response_format=json_schema` + deterministic Python markdown render. Replaces the 6.3.7 regex-sweep approach after blind rating regressed 4.17 → 2.94 on 3 trials (2026-07-30). Structurally eliminates empty-citation-wrapper leaks (`*(Source: )*`, `[N] Label:` empty), bracketed scratch-marker leaks (`[unsupported: ...]`, `[needs citation]`, `[unverified]`, `[not covered]`), and enforces F1–F6 allow-list gate (rubric-orphan claims with no valid citation URL are dropped, not annotated). New module `ops/benchmarks/adr_010/harness/structural_finalize.py` (schema + Ollama call + parser + renderer + prompt builder); 19 new tests. Best-effort with fallback to prior `current_report` on schema error. New CLI `--no-structural-finalize`, kwarg `enable_structural_finalize=True`, banner “Stage 6.3.8”. Whole-repo fast tier: 1180 → 1199 passed (+19 = new tier exactly), 19 skipped unchanged. ADR-007 respected (no plugin touch); ADR-008 not applicable (no MemoryPort write); ADR-010 head-to-head unchanged in structure. Lock-in condition: Colossus 3-trial 6.3.8 blind mean ≥ baseline 4.17 | **Ratified v25** | Stage 6.3.8 |

| ADR-057 | `ADR-057-stage-6-3-zetesis-ui-surface.md` | Stage 6.3 · Zetesis UI route surface — promotes Zetesis descriptor from ADR-052 §Q2=A (zero panels, zero routes, empty design tokens) to a **routes-only** shape adding one `Route(path="/zetesis", label="Zetesis", icon="🔬", lazy_module="zetesis/pages/ResearchPage")`. Zero panels at 6.3 (panels land Stage 6.4 when the kernel FastAPI shell mounts). Locked constants at `plugins/zetesis/plugin.py`: `ZETESIS_ROUTE_PATH="/zetesis"`, `ZETESIS_ROUTE_LABEL="Zetesis"`, `ZETESIS_ROUTE_ICON="🔬"`, `ZETESIS_ROUTE_LAZY_MODULE="zetesis/pages/ResearchPage"`. Descriptor test `test_descriptor_has_zero_routes_at_stage_6_1` amended to `test_descriptor_has_one_route_at_stage_6_3` matching the new invariant. ADR-052 §Q2=A amended with STATUS AMENDMENT block preserving original text (zero-routes was 6.1-specific). Zero new pip deps. `PORTING_LEDGER.md` created at repo root (spec-required file was previously missing; entry logs kernel FastAPI bootstrap as HAND-BUILT). ADR-007 respected (no cross-plugin import). ADR-008 respected (no new MemoryPort write path added). ADR-031 respected (`_derive_parity(routes ∧ panels)` returns IN_PROGRESS at 6.3 — panels land 6.4). ADR-052 amended. ADR-056 preserved (kernel wiring stage unchanged). ADR-054 5.33 baseline preserved (this ADR does not touch the inner loop). `source_diversity ≥ 5` retained as **audit signal only** per user Option B ratification 2026-08-01 — not a Stage 6.4 exit-gate blocker. Also appends Kosmos v25 Addendum (Rules 1–7) to `docs/Kosmos-Build-Spec-v25.md` per user Option C: llama-swap-only model routing, MoltMCP transport pinning, OpenHands ADR-013 vendor floor, Zetesis inner-loop lock, source_diversity audit-only, A2A/AGUI protocol pinning, Kosmos GUI dark-first design lock. DoD anchor `pytest plugins/zetesis/tests/test_zetesis_plugin.py` — 29 tests green after descriptor test rename+rewrite | **Ratified v25** | Stage 6.3 |
| ADR-068 | `ADR-068-stage-1-5-gui-realization-and-backend-gap-ledger.md` | Stage 1.5 · GUI realization scope + backend-gap ledger — realizes the full GUI surface allowed by the current backend across four sequential waves on branch `stage-1-5-gui-realized`. Locks three backend deltas (D1 `GET /api/ollama/status` passthrough to Ollama `/api/ps` with static 32 GiB Colossus VRAM constant; D2 `GET /api/praxis/constitution` read-only; D3 `GET /api/praxis/apex/policies` read-only) required by the persistent shell's model-swap indicator and the `GOVERNANCE` panel real content. Hybrid IA — five job-segmented static pages (`/command`, `/operate`, `/govern`, `/observe`, `/memory`) coexist with plugin-registered routes (`/tektos`, `/zetesis`, `/gnosis`, `/tektos-ui`) so existing Playwright route contracts stay green. Tibetan Tailwind v4 `@theme` tokens (OKLCH; Five Buddha Family palette per `Kosmos-GUI-UX-Design-Spec.md`) hydrated at runtime from `/api/kernel/design-tokens` so future plugin-registered tokens override without a code change. Wave A ships the persistent shell (job-sidebar, top bar with WS algedonic pill + Cmd+K + live model-swap indicator + kill-switch stub, Radix Sheet right drawer scaffold); Wave B ships the approvals-queue full UI over ADR-062; Wave C ships the `GOVERNANCE` panel real content over D2/D3; Wave D ships the Gnosis KG browser + Zetesis research console over ADR-064 + ADR-060, with Cytoscape.js vendored for the KG graph view. Explicitly deferred out of Stage 1.5: real Tektos plan lifecycle (Stage 2 per ADR-067 §Q4=B); `MEMORY_INTEGRITY` / `CONTEXT_PRESSURE` / `HARDWARE_RESILIENCE` panel content (each requires a new port and Stage-TBD ADR); real `MODEL_SWAP_SLO` accountant (as opposed to the passthrough indicator). `kernel/app.py` version 6.5.8 → 6.5.9; zero new port surface; zero new pip dep on the kernel side; ADR-007 respected (class-name matching, no plugin-internal imports leak). ADR-014 preserved (UI Parity Rule). ADR-031 preserved (FrontendContractPort). ADR-034 preserved (TraceFeedPort). ADR-057 preserved (kernel-owned route surface). ADR-062 preserved (approval resolve endpoints). ADR-066 preserved (Stage 6.5.9 GUI enablement). ADR-067 preserved (Stage 1 kernel_ui_glue supersedure — this ADR consumes ADR-067's URL corrections, does not amend them). Lock-in condition: this ADR ratified, `stage-1-5-gui-realized` PR merged, `ui/` builds green via `next build`, all Playwright test tiers including new `09-persistent-shell.spec.ts` green | **Ratified v25** | Stage 1.5 |
| ADR-067 | `ADR-067-stage-1-gui-glue-router-superseded.md` | Stage 1 · GUI shell — supersedes `Kosmos-gui-build-spec-v1.md` §5 `kernel_ui_glue` router. Cross-reference against `kernel/app.py` at `3197b6d` (Stage 6.5.9 merged) shows every glue-router endpoint already lives at the identical `/api/*` path on the kernel FastAPI app (schema/routes/panels/plugins/design-tokens, approvals + approve/reject, resources balances/queue, phrouros anomalies, tektos turn, notifications health/slo/ack, zetesis research, gnosis query/corpora/stats/event, algedonic + events WebSockets). Spec's mount block references non-existent module-level names — real adapter access flows through `registry.*`. Decision: (D1) `kernel_ui_glue/` package NOT included in Stage 1 GUI scaffold; UI clients target existing `/api/*` endpoints directly. (D2) Gnosis Stage 4.6 gate mount at `/gnosis-gate` IS retained (distinct ASGI app, no route duplication). (D3) `ui/lib/kernel-client.ts` URLs corrected: `/api/kernel/tokens` → `/api/kernel/design-tokens`, `/api/approvals/{id}/resolve` (POST) → `/api/approvals/{id}/{approve,reject}`, `/ws/algedonic` → `/api/algedonic/ws`. (D4) `/api/tektos/plan/{id}[/approve|/execute|/diff]` UI wiring deferred to Stage 2 pending a Tektos-plan-surface ADR (kernel only exposes `/api/tektos/turn` at 6.5.6). (D5) `Kosmos-gui-build-spec-v1.md` §5 gets a header note referencing this ADR. Newer-wins rule from `kosmos-spec-diff`: pre-existing ratified `/api/*` routes are the newer, ratified reality; spec §5 predates them. Zero new port surface; zero new file under `adapters/`; zero `PORTING_LEDGER.md` change; zero new pip dep. ADR-007 respected (no plugin-to-plugin imports introduced). ADR-031/ADR-057/ADR-058/ADR-059/ADR-060/ADR-061/ADR-062/ADR-063/ADR-064/ADR-065/ADR-066 all preserved (this ADR consumes them, does not amend them). Lock-in condition: this ADR ratified, Stage 1 GUI PR merged, `ui/` builds green via `next build`, all Playwright test tiers green | **Ratified v25** | Stage 1 |
| ADR-066 | `ADR-066-stage-6-5-9-gui-enablement.md` | Stage 6.5.9 · GUI enablement kernel additions — four additive routes + one Tektos-UI template fix closing the last kernel gaps against `Kosmos-gui-build-spec-v1.md` §1. Locks five decisions: (D1) `POST /api/notifications/{notification_id}/ack` — passthrough to `NotificationPort.ack_receipt(notification_id, subscriber_id)`; body `{subscriber_id: <non-empty str>}`; 200 returns `{acked: bool}`; 400 on missing/empty subscriber_id or malformed body; 502 on upstream exception; 503 when subsystem down. (D2) `GET /api/resources/queue?kind=<ResourceKind>&n=<int>` — passthrough to `ResourcePort.peek(kind, n)`; 400 on unknown kind or `n` out of `[1, 100]`; 200 returns `list[QueuedRequest]`; 502 on upstream exception; 503 when subsystem down. (D3) `WebSocket /api/algedonic/ws` — accepts, sends `{frame: "ready"}`, registers a kernel-internal `_WebSocketAlgedonicSink` (implements `ports.notification.Sink`) that forwards only `AlgedonicTier.ALGEDONIC` records as `{frame: "algedonic", record: {...}}`; non-algedonic tiers soft-drop (return True); transport errors soft-fail (return False); sink is unregistered best-effort on disconnect; closes with 1011 when notification subsystem is down. Routes through `NotificationPort.register_sink`/`unregister_sink` — no `adapters/notification/` change; algedonic push bypasses the event bus by design per spec §30 to preserve the 500ms delivery SLO. (D4) `GET /api/notifications/slo` alias — second route decorator on the existing `notification_health()` handler, byte-identical response; `/api/notifications/health` remains live so existing tests + clients don't break. (D5) Tektos-UI htmx template fix — new `TEKTOS_UI_HTMX_JS_TEMPLATE_HREF = "htmx.min.js"` (bare, mount-relative) in `plugins/tektos/ui/policy.py`; `plugins/tektos/ui/templates.py` swaps the `<script src="{htmx_src}">` binding from `TEKTOS_UI_HTMX_JS_PATH` (root-relative, `/htmx.min.js`) to the new relative constant. Sub-app route decorator (`@app.get("/htmx.min.js")`) unchanged; browsers now resolve the relative href against the sub-app mount root (`/tektos-ui/`) → `/tektos-ui/htmx.min.js` → 200. Closes the `KNOWN_ISSUES.md` 2026-08-01 htmx entry (moved to `DEBUG_LOG.md` as closed diagnosis). `kernel/app.py` version 6.5.8 → 6.5.9. Zero new port surface; zero new file under `adapters/`; zero `PORTING_LEDGER.md` change; zero new pip dep. ADR-007 respected (class-name matching; kernel does not import notification/resource adapter internals). ADR-008 preserved (algedonic sink writes go through `NotificationPort` — zero-trust floor at port layer). ADR-029 preserved (`ResourcePort` protocol untouched). ADR-034 preserved. ADR-045 preserved (Tektos UI sub-app route surface untouched — D5 is a template binding change only). ADR-057 preserved (kernel-owned route surface). ADR-062 preserved (approval resolve endpoints; error-mapping pattern reused for 400/502/503). ADR-063 preserved (`registry.notification`, `registry.resource` singletons reused). ADR-064 preserved (Gnosis routes unaffected). ADR-065 preserved (Stage 6.5.8 Tektos UI kernel mount unchanged; D5 fix is scoped inside the sub-app). DoD anchor `pytest tests/kernel/test_stage_6_5_9_gui_enablement.py` — fast integration + sink-unit tests: D1 (7) happy path, acked-false, missing/empty subscriber_id, non-object body, notification-down 503, upstream-exception 502; D2 (6) happy path, default n=5, unknown kind 400, out-of-range n 400, resource-down 503, upstream-exception 502; D3 unit tier (4) algedonic tier sent, non-algedonic soft-dropped, transport-error soft-fail, close is noop; D3 route tier (3) ready frame + sink registration, notification-down closes WS, sink unregistered on disconnect; D4 (2) slo alias equals health, slo returns 503 when down; D5 (3) relative-href constant, route-constant unchanged, index template emits relative src. Lock-in condition: this ADR ratified, all tests green on Colossus, four routes reachable, htmx template fix verified in served HTML, tag `stage-6-5-9-gui-enablement` pushed. Stage 1 GUI (Next.js shell) unblocked | **Ratified v25** | Stage 6.5.9 |
| ADR-065 | `ADR-065-stage-6-5-8-tektos-ui-kernel-mount.md` | Stage 6.5.8 · Tektos UI kernel mount — mounts the Stage 3.11 `build_tektos_ui_app` sub-app at `/tektos-ui/*` inside `kernel/app.py` lifespan. **Option B independent mount:** depends only on `registry.approval` (ADR-062) + `registry.memory` (ADR-063), NOT on `registry.tektos` (the agent plugin), so the change-approval UI stays reachable during LLM/agent outages for triage of stuck plans. Locks eight decisions: (D1) route surface — seven sub-app routes under `/tektos-ui/*`: `/`, `/plan/{approval_id}`, `/plan/{approval_id}/approve`, `/plan/{approval_id}/execute`, `/plan/{approval_id}/diff`, `/healthz`, `/htmx.min.js` (path constants from `plugins/tektos/ui/policy.py`, kernel does not re-declare). (D2) boot dependencies — `approval` + `memory` only; missing dependency records `registry.errors['tektos_ui']` and sets `registry.tektos_ui = None`; `/health.subsystems.tektos_ui = false`; kernel keeps 200. (D3) executor binding — `NopExecutor` (`plugins.tektos.ui.executor.NopExecutor`) at Stage 6.5.8; deterministic canned diff `tektos:plan:before` → `tektos:plan:after` with SHA-256; no filesystem writes; route handler owns the `MemoryPort.write_event` audit write; real executor (git-worktree diff-applier + rootless container command runner) arrives at Stage 3.12 as swap-in with zero kernel change. (D4) `_BootRegistry` gains two fields — `tektos_ui: FastAPI \| None`, `tektos_ui_executor: ExecutorPort \| None`. (D5) `/health.subsystems` gains `tektos_ui: bool`; existing subsystems unchanged. (D6) `kernel/app.py` version 6.5.7 → 6.5.8. (D7) import discipline — `plugins.tektos.ui.server.build_tektos_ui_app` + `plugins.tektos.ui.executor.NopExecutor` imported only inside the boot try/except; class-name matching for any UI-internal errors per ADR-007. (D8) zero new port surface (`ApprovalResolverPort`, `MemoryPort`, `ExecutorPort` all pre-existing); zero new file under `adapters/`; zero `PORTING_LEDGER.md` change (htmx already vendored per ADR-045); zero new pip dep. ADR-007 respected (no plugin-to-plugin import; UI reads through `ApprovalResolverPort` and `MemoryPort` only). ADR-008 respected (`MemoryPort.write_event` with `provenance='tektos_ui'` + confidence — zero-trust floor preserved via server route, not kernel). ADR-045 preserved (`ApprovalResolverPort` protocol untouched). ADR-057 preserved (kernel-owned route surface; sub-app mount owned by kernel). ADR-062 preserved (approval resolve endpoints; UI is an alternate client of the same port). ADR-063 preserved (`registry.tektos` boot block unchanged; UI mount is a peer, not a child). ADR-064 preserved (Gnosis routes unaffected). DoD anchor `pytest tests/kernel/test_stage_6_5_8_tektos_ui_mount.py` — 12 fast integration tests across three tiers: kernel-boot tier (`/health.subsystems.tektos_ui = true`, `/tektos-ui/healthz` returns 200, `registry.tektos_ui_executor.__class__.__name__ == 'NopExecutor'`, `app.routes` contains a Mount at `/tektos-ui`); sub-app contract tier via direct `build_tektos_ui_app` with fake `ApprovalResolverPort` + `MemoryPort` + `ExecutorPort` (index 200, plan detail known-id 200 / unknown-id 404, approve calls `resolver.resolve(approval_id, True, resolved_by='tektos_ui')` + writes `TEKTOS_UI_PLAN_APPROVED_PREDICATE` event with `provenance='tektos_ui'`, execute writes `TEKTOS_UI_PLAN_EXECUTED_PREDICATE` with `diff_sha256`, diff writes `TEKTOS_UI_PLAN_DIFF_RENDERED_PREDICATE`); boot-degradation tier (missing `approval` records error + `tektos_ui = None`, missing `memory` same). Lock-in condition: this ADR ratified, all tests green on Colossus, `curl -sI http://127.0.0.1:8000/tektos-ui/healthz` returns 200, `/health.subsystems.tektos_ui = true`, tag `stage-6-5-8-tektos-ui-mount` pushed | **Ratified** | Stage 6.5.8 |
| ADR-064 | `ADR-064-stage-6-5-7-gnosis-retrieval-surrogate.md` | Stage 6.5.7 · Gnosis retrieval surrogate HTTP mount + boot seeder — four read-only `/api/gnosis/*` routes over the kernel's `MemoryPort` singleton plus an env-gated `KOSMOS_GNOSIS_SEED=1` boot seeder that ingests `ALL_CORPORA` (5 corpora, ~40 facts) via `MemoryPort.write_event`. Locks five decisions: (D1) route surface — `GET /api/gnosis/query?q&as_of&limit&corpus` (limit `[1,100]`, `as_of` optional tz-aware ISO-8601, `corpus` optional and validated against manifest, translated to payload-side `provenance` filter with widened raw limit `min(100, limit*5)`); `GET /api/gnosis/corpora` (manifest augmented with live `fact_count` + `last_ingested_at` from seeder, static fallback from `len(corpus.facts)`); `GET /api/gnosis/stats` (top-line dashboard numbers from `ALL_CORPORA` — safe when memory is down); `GET /api/gnosis/event/{event_id}` (`^[A-Za-z0-9._:-]+$`, 404 on miss). (D2) manifest schema — `{name, provenance_predicate, summary, stage, fact_count, last_ingested_at}` with `provenance_predicate` unique across all five entries and used as the corpus-filter join key. (D3) boot seeder — env-gated (`KOSMOS_GNOSIS_SEED=1`, default off) so CI + test tiers stay deterministic; iterates `ALL_CORPORA` calling `MemoryPort.write_event(subject, predicate, object, provenance=..., confidence=..., attributes={..., corpus_name, corpus_event_id, as_of})`; idempotent by class-name matching (`_GNOSIS_SEED_IGNORABLE = {MemoryWriteBlocked, ClientError, ConstraintValidationFailed}`) so re-runs on populated DBs are no-ops; records `registry.gnosis_corpus_counts` + `registry.gnosis_last_seeded_at`; failures record `registry.errors["gnosis_seed"]` and don't block boot. (D4) error mapping — 503 when `registry.memory is None`; 400 on validation (`empty q`, `limit out of range`, `as_of naive/malformed`, `unknown corpus`, malformed `event_id`); 400 on `ValueError` from adapter; 502 on upstream `Exception` (class-name preserved in detail); 404 on `event/{id}` miss. Class-name matching preserves ADR-007 (no Gnosis/Graphiti exception imports in `kernel/app.py`). (D5) `kernel/app.py` version 6.5.6 → 6.5.7; `_BootRegistry` gains two fields (`gnosis_corpus_counts: dict[str, int]`, `gnosis_last_seeded_at: str \| None`); `/health.subsystems` unchanged (Gnosis surrogate reuses `memory` bool by design). Zero new port surface; zero new file under `adapters/`; zero PORTING_LEDGER change; zero new pip dep. ADR-007 respected (class-name matching). ADR-008 respected (writes go through `MemoryPort.write_event` with provenance + confidence — zero-trust floor preserved). ADR-016 preserved (Knowsys→Gnosis merge — surrogate is compatible with a future `plugins/gnosis/` package). ADR-047 preserved (hybrid tier). ADR-051 preserved (surrogate pattern — corpora locate at adapter fixtures layer, router enumerates statically). ADR-057 preserved (kernel-owned route surface). ADR-063 preserved (`registry.memory` singleton reused). DoD anchor `pytest tests/kernel/test_stage_6_5_7_gnosis_retrieval.py` — fast integration tests over `_FakeMemoryPort`: query happy path; empty `q` → 400; `limit` out of range → 400; `as_of` tz-aware ok; `as_of` naive → 400; `as_of` malformed → 400; corpus filter matches by provenance; unknown corpus → 400; memory down → 503; upstream exception → 502; upstream `ValueError` → 400; manifest shape with static fallback counts; manifest reflects seeded counts; provenance predicates unique across manifest; stats shape (all 8 keys, `earliest <= latest`); event happy path; event miss → 404; malformed event id → 400; event memory down → 503; seeder idempotent on `MemoryWriteBlocked` (all writes attempted, counts zero); seeder happy path writes every fact with provenance + confidence. Lock-in condition: this ADR ratified, all tests green on Colossus, four Gnosis routes reachable under `/api/kernel/routes`, `KOSMOS_GNOSIS_SEED=1` seeds `ALL_CORPORA` idempotently on real DozerDB | **Ratified** | Stage 6.5.7 |
| ADR-063 | `ADR-063-stage-6-5-6-tektos-kernel-mount.md` | Stage 6.5.6 · Tektos kernel mount + turn endpoint — mounts `TektosPlugin` on the kernel, promotes `LLMPort` + `MemoryPort` to registry-owned singletons, and opens `POST /api/tektos/turn` over `TektosAgent.run()`. Locks eight decisions: (D1) five new registry fields — `llm: LLMPort` (OllamaAdapter), `memory: MemoryPort` (DozerDbMemoryAdapter, env-gated backends), `tektos: TektosPlugin`, `tektos_agent: TektosAgent`, `tektos_agent_lock: asyncio.Lock`. (D2) boot order — `llm` (via `_boot_llm` closure, env vars `KOSMOS_OLLAMA_BASE_URL` default `http://127.0.0.1:11434/v1` + `KOSMOS_TEKTOS_MODEL` default `qwen2.5:32b-instruct-q4_K_M`) → `memory` (via `_boot_memory` closure; `KOSMOS_MEMORY_BACKEND=in_memory` default uses `InMemoryGraphBackend` + `InMemoryTemporalIndex` + `NoOpAmgPolicy` for CI/test safety; `=dozerdb` uses `DozerDbGraphBackend` + `GraphitiTemporalIndex` + `AmgGuardPolicy(tiered)` requiring `KOSMOS_DOZERDB_URI/_USER/_PASSWORD` and optional `_DATABASE`, plus `KOSMOS_EMBED_MODEL` default `nomic-embed-text`) → Tektos mount (depends on `frontend_contract` + `llm` + `memory`; missing dependency records reason on `registry.errors['tektos']` and continues; else instantiates `TektosPlugin` + awaits `.start()` + constructs `TektosAgent` + creates `asyncio.Lock`). (D3) shutdown — `tektos.stop()` + `llm.close()` before existing zetesis stop, best-effort. (D4) endpoint `POST /api/tektos/turn` body `{content: <non-empty str>}` → 200 with `TektosStep` JSON via `_dataclass_to_dict`; serialized across concurrent requests via `registry.tektos_agent_lock` so caller never sees `TektosAgentAlreadyRunError`; 400 on missing/empty/non-string content, non-JSON body, `ValueError`, or `TektosAgent*Error`; 502 on upstream adapter failure (Ollama unreachable, memory write failure); 503 when subsystem down. Class-name matching (`type(exc).__name__`) keeps `TektosAgent*Error` imports out of `kernel/app.py` per ADR-007 (ADR-062 precedent). (D5) `/health.subsystems` gains three bools: `llm`, `memory`, `tektos`; `boot_errors["tektos"|"llm"|"memory"]` surface via same `registry.errors` map. (D6) kernel imports `plugins.tektos.plugin.TektosPlugin` + `plugins.tektos.agent.TektosAgent` only inside the boot block; endpoint handler catches by class name. (D7) `kernel/app.py` version 6.5.5 → 6.5.6. (D8) tests use monkey-patched `_FakeLLMPort` + `_FakeMemoryPort` (matching `plugins/tektos/tests/test_tektos_agent.py`) swapped into `registry.llm/.memory/.tektos_agent/.tektos_agent_lock` after `TestClient` startup so integration tests never hit Ollama or the graph backend. Zero new port surface; zero new file under `adapters/`; zero `PORTING_LEDGER.md` change (adapters already `VENDORED` per ADR-058). ADR-007 respected. ADR-031 preserved (descriptor shape). ADR-036 preserved (Tektos agent Stage 3.1). ADR-041 preserved (Tektos descriptor Stage 3.7). ADR-057 preserved (kernel-owned route surface). ADR-058 preserved (kernel-shared adapters — this ADR extends the sharing pattern from event_bus/resource/notification to llm/memory). DoD anchor `pytest tests/kernel/test_stage_6_5_6_tektos_turn.py` — fast integration tests: turn returns valid `TektosStep`; concurrent requests both succeed (lock serialization); missing/empty/non-string content → 400; non-JSON body → 400; LLM failure → 502; memory failure → 502; `/health` reports `tektos/llm/memory`; `/api/kernel/plugins` lists `tektos`; forced-down subsystem → 503. Lock-in condition: this ADR ratified, `/api/tektos/turn` returns 200 with a valid `TektosStep` on Colossus against real Ollama + DozerDB, and `/api/kernel/plugins` lists `tektos` | **Ratified** | Stage 6.5.6 |
| ADR-062 | `ADR-062-stage-6-5-5-approval-resolve-endpoints.md` | Stage 6.5.5 · Approval resolve endpoints — adds `POST /api/approvals/{approval_id}/approve` and `POST /api/approvals/{approval_id}/reject` over the existing `ApprovalResolverPort` (ADR-045). Locks seven decisions: (D1) two POST routes with optional JSON body; approve accepts `{reason?, modifications?, resolved_by?}`, reject requires `{reason: non-empty str, resolved_by?}`; `resolved_by` defaults to `"user"`; approve with non-empty `modifications` object transitions to `MODIFIED`, else `APPROVED`. (D2) status codes — 200 on success returning updated `ApprovalRecord` via `_dataclass_to_dict`; 400 on validation failure (bad JSON body, non-string reason, reject-without-reason, `ValueError` from engine); 404 on `ApprovalNotFoundError`; 409 on `InvalidTransitionError` (already-resolved is a caller bug); 503 when subsystem down; 500 otherwise. Class-name matching (`type(exc).__name__`) distinguishes Praxis APEX exceptions without importing `plugins.praxis.apex.errors` from the kernel (ADR-007). `ValueError` matches via `isinstance` since it's stdlib. (D3) kernel-owned route surface per ADR-057 §Q7=B; zero new port surface; zero new file under `adapters/`; zero `PORTING_LEDGER.md` change. (D4) body validation — missing body → `{}`; must be JSON object; `reason` must be string if present; `modifications` must be JSON object if present; `resolved_by` must be non-empty string. Kernel-side reject-reason enforcement short-circuits before delegating to the engine's identical rule. (D5) `kernel/app.py` version 6.5.4 → 6.5.5. (D6) tests via kernel's live `registry.approval._engine.propose(...)` on `TestClient.portal.call` to seed `HUMAN_REVIEW` records — chosen over `HUMAN_REQUIRED` since the latter schedules an escalating algedonic cadence; `HUMAN_REVIEW` only schedules one 4-hour review-missed callback, safe for fast tests. (D7) non-changes — `ApprovalResolverPort` protocol untouched, `PraxisApprovalResolverAdapter` untouched, `KernelChangeApprovalAdapter` untouched; zero new pip dep. ADR-007 respected. ADR-033 preserved (three-tier ladder). ADR-037 preserved (`ApprovalGatewayPort` untouched). ADR-045 preserved (port surface untouched). ADR-057 preserved. ADR-061 preserved (WS bridge unchanged; a Stage 6.6 mapping ADR will bridge Praxis `apex.intention.*` events to GUI-facing `approval.*` event vocabulary). DoD anchor `pytest tests/kernel/test_stage_6_5_5_approval_resolve_endpoints.py` — fast integration tests: approve happy path (→ APPROVED); approve with modifications (→ MODIFIED with `resolved_by=gui`); reject happy path (→ REJECTED); reject without reason → 400; reject with whitespace-only reason → 400; approve with bad JSON body → 400; approve with non-object body → 400; approve with non-string reason → 400; approve unknown id → 404; reject unknown id → 404; double resolve → 409. Lock-in condition: this ADR ratified, all tests green on Colossus, both endpoints reachable under `/api/kernel/routes` | **Ratified** | Stage 6.5.5 |
| ADR-061 | `ADR-061-stage-6-5-4-websocket-event-bus-bridge.md` | Stage 6.5.4 · WebSocket event-bus bridge (`/api/events/ws`) — persistent multi-event feed for the GUI over the in-process `EventBusPort` fan-out. Locks eight decisions: (D1) endpoint `GET /api/events/ws`; kernel-owned per ADR-057 §Q7=B; query param `types=a,b,c` selects subscription set, absent → default set. (D2) `WS_DEFAULT_EVENT_TYPES` = (`phrouros.anomaly.detected`, `zetesis.research.started`, `zetesis.research.completed`); expands stage-by-stage as new publishers land (6.5.5 adds `approval.requested`/`approval.resolved`). (D3) concurrency — one `event_bus.subscribe(t, maxsize=256)` per requested type + one forwarder task per queue + a `_drain_client` task for prompt disconnect detection; `asyncio.wait(return_when=FIRST_COMPLETED)` unblocks on disconnect or forwarder error; cancels the rest; finally-block unsubscribes each queue best-effort. (D4) backpressure — `maxsize=256` per queue; full queue drops per Valkey adapter's existing `eventbus_local_queue_full` log; slow WS consumer cannot stall publisher or other subscribers. (D5) auth — none; single-user local-first inheritance. (D6) wire format — JSON text frames only; first frame `{frame: "ready", subscribed: [...]}`; subsequent frames `{frame: "event", envelope: {event_type, producer_plugin, payload, event_id, occurred_at (ISO), schema_version}}`; client→server frames ignored except `close()`. (D7) `kernel/app.py` version 6.5.3 → 6.5.4. (D8) tests via `TestClient.websocket_connect(...)` + `registry.event_bus.publish(EventEnvelope(...))`. Zero new port surface; zero new file under `adapters/`; zero `PORTING_LEDGER.md` change. `EventBusPort` protocol untouched; `EventEnvelope` untouched. ADR-007 respected. ADR-008 not applicable. ADR-023 preserved (envelope-first, in-process fan-out contract). ADR-024 explicitly deferred (cross-process consumer-group semantics). ADR-057 preserved. ADR-060 preserved (SSE endpoint for the one-shot research response is companion, not replaced). DoD anchor `pytest tests/kernel/test_stage_6_5_4_websocket_event_bus_bridge.py` — 10 fast integration tests: `_parse_ws_types` handles None/empty/dedup/all-empty; `ready` frame with default and custom subscription set; forwards event on subscribed type with ISO `occurred_at`; does not forward unsubscribed type; forwards multiple types concurrently; unsubscribes on disconnect. Lock-in condition: this ADR ratified, all tests green on Colossus, `GET /api/events/ws` accepts WebSocket connections and forwards published envelopes | **Ratified** | Stage 6.5.4 |
| ADR-060 | `ADR-060-stage-6-5-3-zetesis-research-sse.md` | Stage 6.5.3 · Zetesis `/research` SSE endpoint — GUI-unblocking route surface over the already-shipped `ZetesisPlugin.research()`. Locks four decisions: (D1) endpoint shape `POST /api/zetesis/research` returning `text/event-stream` with `started` + `completed` events on happy path, `started` + `error` on failure; kernel-owned per ADR-057 §Q7=B; request body carries mandatory `query:str` and optional partial `config:object` mapped over `ZetesisResearchConfig` defaults via `dataclasses.replace`. (D2) block-await concurrency — no background task, no polling, no cancellation hook; single-user local-first pattern; per-node LangGraph progress deferred to a later slice when the inner loop emits telemetry through `EventBusPort`. (D3) config passthrough coerces `priority_class` (str → `PriorityClass[value.upper()]`), `compute_budget` (num/str → `Decimal`), `fact_anchor_urls`/`rubric_lines` (list → tuple); unknown keys dropped for forward-compat; invalid coercion → 400 before SSE handshake, not mid-stream `error`. (D4) `kernel/app.py` version 6.5.2 → 6.5.3. Zero new port surface; zero new file under `adapters/`; zero `PORTING_LEDGER.md` change. `ZetesisPlugin.research()` untouched; `ResearchReport` dataclass untouched. ADR-007 respected. ADR-008 not applicable. ADR-057 preserved (route surface owned by kernel). ADR-058 preserved (Zetesis mount block unchanged). ADR-059 preserved (Phrouros wire + resource seed unchanged). DoD anchor `pytest tests/kernel/test_stage_6_5_3_zetesis_research_sse.py` — 8 fast integration tests: happy path emits `started` then `completed` with `ResearchReport` payload; error path emits `started` then `error` with `error_type`; config passthrough coerces `priority_class`/`compute_budget`/`fact_anchor_urls`; 400 on missing/empty query; 400 on bad `priority_class`; 400 on bad `compute_budget`; 503 when Zetesis subsystem is down. Lock-in condition: this ADR ratified, all 8 tests green on Colossus, endpoint mounted under `/api/kernel/routes` on `/health` reporting `zetesis: true` | **Ratified** | Stage 6.5.3 |
| ADR-059 | `ADR-059-stage-6-5-1-2-phrouros-wire-and-resource-seed.md` | Stage 6.5.1 + 6.5.2 · Phrouros wire + resource seed — GUI-unblocking kernel-side glue over already-shipped primitives. Locks three decisions: (D1) `PhrourosEngine` wires on kernel start over `ports.trace_feed.InMemoryTraceFeedAdapter()` with `detectors=(LoopDetector(),)` only — the three skeleton detectors raise `DetectorNotImplementedError` per `plugins/phrouros/detector.py` docstring; `UnauthorizedToolDetector` requires a curated tool allowlist not yet defined at kernel level. Failure is graceful (`registry.errors["phrouros"]`; `/health.subsystems.phrouros == False`; kernel 200). (D2) `SqliteResourceAdapter` is seeded at boot with the six canonical `ResourceKind` values from `KERNEL_RESOURCE_SEED` — `time=1440`, `money=100.00`, `attention=100`, `compute=100`, `knowledge=1` (replenish rejects amount ≤ 0), `energy=100`. Seed failure best-effort (`registry.errors["resource_seed"]`; resource subsystem stays up). Values are presentation defaults for GUI resource-meter widgets, NOT commitments about physical resources. (D3) `kernel/app.py` version 6.5.0 → 6.5.2 (composite of both sub-stages). Zero new port surface; zero new file under `adapters/`; zero PORTING_LEDGER change. ADR-007 respected. ADR-008 not applicable (no MemoryPort write path added). ADR-029 preserved (seed uses public `replenish()` verb). ADR-034 preserved (Phrouros contract unchanged — detectors tuple filtered, not amended). ADR-058 preserved (Zetesis mount block unchanged). DoD anchor `pytest tests/kernel/test_stage_6_5_1_2_phrouros_and_seed.py` — 5 fast integration tests: `/health.subsystems.phrouros` is True, `/api/phrouros/anomalies` returns 200 with `[]` on boot, publishing 6 identical `TraceEvent`s into `registry.trace_feed` produces a `loop_detector` anomaly visible on `/api/phrouros/anomalies`, `/api/resources/balances` returns non-None `ResourceBalance` for all six canonical kinds, seed values match `KERNEL_RESOURCE_SEED`. Lock-in condition: this ADR ratified, all 5 tests green on Colossus, `/api/phrouros/anomalies` returns 200 (no longer 503), `/api/resources/balances` returns non-null balances for all six kinds | **Ratified** | Stage 6.5.1+6.5.2 |
| ADR-058 | `ADR-058-stage-6-5-zetesis-kernel-mount.md` | Stage 6.5 · Zetesis kernel mount — plugin becomes a seventh subsystem in `kernel/app.py` lifespan behind per-subsystem try/except (matching ADR-057 boot pattern). Locks three decisions: (D1) graceful-per-subsystem backend policy — plugin mount failure surfaces under `registry.errors["zetesis"]` and returns `zetesis: false` on `/health.subsystems`, kernel keeps 200 on all other endpoints; (D2) five previously-stubbed ports at 6.3.9 (MemoryPort/VectorPort/DataPort/ResourcePort/NotificationPort) bind to real adapter classes with in-memory or shared backends — `DozerDbMemoryAdapter` + `InMemoryGraphBackend` + `InMemoryTemporalIndex` + `NoOpAmgPolicy`; `QdrantVectorAdapter` + `InMemoryQdrantBackend`; `FilesystemDataAdapter` rooted at `~/.local/state/kosmos/data`; kernel-shared `SqliteResourceAdapter` + `KernelNotificationAdapter`. Real DozerDB/Graphiti/AMG backends attach at Stage 6.5.1 once neo4j-compose lands on Colossus; ADR-054 5.33/6 rater trial reruns at 6.5.1 against real MemoryPort backing. (D3) FrontendContractPort + EventBusPort + ResourcePort + NotificationPort reuse the kernel's live adapter instances — sharing FC is required for the descriptor to appear on `/api/kernel/plugins` and `/api/kernel/routes` (FC holds state per-instance); sharing event_bus/resource/notification preferred to avoid fragmenting kernel state on Colossus's fixed envelope. New `build_stage_6_5_zetesis_plugin(*, frontend_contract=None, event_bus=None, resource=None, notification=None, ...)` factory in `plugins/zetesis/adapters/real/factory.py` — 6.3.9 factory preserved verbatim for ADR-054 trial parity. `kernel/app.py` version 6.4.0 → 6.5.0; `_BootRegistry` gains `zetesis` field; `/health.subsystems` gains `zetesis` bool; shutdown calls `plugin.stop()` before event_bus close. `PORTING_LEDGER.md` promotes DozerDbMemoryAdapter, QdrantVectorAdapter, FilesystemDataAdapter, OllamaAdapter, SearxngAdapter, OtelStackObservabilityAdapter from `PLANNED`/`VENDORED` to `WIRED` at Stage 6.5. ADR-007 respected (no cross-plugin import). ADR-008 respected (MemoryPort zero-trust guard preserved at port layer). ADR-031 respected (`_derive_parity(routes ∧ panels)` unchanged at 6.5 — panels land later). ADR-052/§Q3=A skeleton discharged; §Q7=B-plus port surface bound to real adapters. ADR-054 5.33 baseline preserved (this ADR does not touch the inner loop). ADR-056 preserved (Zetesis kernel wiring stage). ADR-057 preserved (route surface untouched). DoD anchor `pytest plugins/zetesis/tests/test_stage_6_5_zetesis_mount.py` — 6 fast integration tests: health reports zetesis up, `/api/kernel/plugins` lists `kosmos.plugin.zetesis`, `/api/kernel/routes` contains `/zetesis`, all 6.4 endpoints still 200, registration holds after start, factory wires all 10 ports to non-stub adapters. Lock-in condition: this ADR ratified, `bin/kernel-smoke-11.sh` returns 200 for all endpoints on Colossus, `/api/kernel/plugins` returns a non-empty list containing `kosmos.plugin.zetesis`, tag `stage-6-5-zetesis-mount` pushed | **Ratified v25** | Stage 6.5 |

| ADR-069 | `ADR-069-stage-1-5-kernel-kill-switch.md` | Stage 1.5 Wave C · Kernel Kill-Switch (Soft Suspend/Resume) — wires the persistent-shell kill-switch to a real backend control with soft-suspend semantics preserving UX Design Spec intent (visible suspended state, one-click resume). Locks six decisions: (D1) `POST /api/kernel/kill` idempotent, optional `{reason?: str}`, returns `{status: "suspended", suspended_at, reason}`. (D2) `POST /api/kernel/resume` idempotent, returns `{status: "running", resumed_at}`. (D3) `GET /api/kernel/suspension` read-only status, never 503. (D4) FastAPI middleware asymmetric gate — allows `/health`, `/api/kernel/**`, `/api/kernel/resume`, WS, HEAD/OPTIONS in suspended state; rejects other mutating/GET-under-/api/ routes with 503 `{detail: "kernel suspended", suspended_at, reason}`. (D5) publishes `EventEnvelope` `kernel.suspended`/`kernel.resumed` on transitions via existing event bus; extends `WS_DEFAULT_EVENT_TYPES` by both. (D6) `kernel/app.py` version 6.5.9 → 6.6.0. Rejects hard process-kill (prevents UI showing suspended state), event-loop freeze (same visible effect), and per-plugin pause flags (violates ADR-007). Zero new ports; zero PORTING_LEDGER change; zero plugin coupling. ADR-007 preserved (kernel-level middleware). ADR-057 preserved (kernel-owned route surface). ADR-061 preserved (WS envelope wire format unchanged — additive event types only). Lock-in condition: this ADR ratified, all pytest cases in `tests/kernel/test_stage_1_5_adr_069_kill_switch.py` green on Colossus, Playwright `11-kill-switch.spec.ts` green, `kernel/app.py.version == "6.6.0"`, `WS_DEFAULT_EVENT_TYPES` includes both `kernel.suspended` and `kernel.resumed` | **Ratified v25 (2026-08-01)** | Stage 1.5 Wave C |
| ADR-070 | `ADR-070-stage-1-5-memory-integrity-graph.md` | Stage 1.5 Wave D · MEMORY_INTEGRITY graph — lands the interactive knowledge-graph panel at `/memory` by extending the Gnosis surrogate (ADR-064) with three read-only graph endpoints and vendoring cytoscape.js for the client. Locks six decisions: (D1) three kernel-mounted read-only routes `GET /api/gnosis/graph/{nodes,edges,node/{id}}` with opaque-cursor pagination, `limit` bounded `[1,100]`; nodes carry `kind` (`subject`/`object`/`zetesis_report`), edges carry `kind` (CIDOC-CRM predicate verbatim, or `zetesis_cited_by`/`zetesis_evidences`); provenance + confidence surfaced on every node and edge, never fabricated. (D2) Union data source — MemoryPort triples via `registry.memory.query_temporal` deduped by exact subject/object string, plus Zetesis `ResearchReport` provenance chains keyed by `trial_id`; `citations` become `zetesis_cited_by` edges to object nodes, `memory_event_id` becomes a `zetesis_evidences` edge back into MemoryPort. (D3) cytoscape.js (`^3.30.0`, MIT) + react-cytoscapejs (`^2.0.0`, MIT) vendored via pnpm; consumed exclusively inside `ui/components/panels/MemoryIntegrityPanel.tsx`; `cose` layout for ≤150 nodes; Louvain community collapse toggle present but no-op in Wave D (Wave E). (D4) `/memory` `PanelGrid` swap `PlaceholderPanel` → `MemoryIntegrityPanel`, `MEMORY_INTEGRITY` always-render special-case (like `AGENT_TRACE`/`GOVERNANCE`); corpus dropdown defaults to "all"; empty state `role="status"`, error state `role="alert"` with class-name only. (D5) Zetesis integration best-effort — when `registry.zetesis_plugin is None` or `registry.zetesis_reports` empty, endpoints degrade to MemoryPort-only without 503; `registry.zetesis_reports` is a `deque(maxlen=100)` populated by best-effort event bus subscriber added on Zetesis mount; confidence for zetesis nodes = `1.0` if `error is None` else `0.0`. (D6) `kernel/app.py` version 6.6.0 → 6.7.0; `_BootRegistry.zetesis_reports` field; `WS_DEFAULT_EVENT_TYPES` unchanged. Zero new ports; zero plugin coupling (ADR-007 preserved — kernel-owned routes only). ADR-057 preserved (kernel-owned route surface). ADR-064 extended additively (retrieval `/query` untouched). ADR-069 preserved (graph endpoints correctly gate 503 under `/api/**` when suspended). Rejects extending `/api/gnosis/query` (breaks retrieval semantics), spawning `plugins/gnosis/` now (ADR-064 defers to Phase 3), d3-force/vis-network/sigma.js (spec calls out cytoscape.js). Lock-in condition: this ADR ratified, all pytest cases in `tests/kernel/test_stage_1_5_adr_070_gnosis_graph.py` green on Colossus, Playwright `12-memory-integrity-graph.spec.ts` green, `kernel/app.py.version == "6.7.0"`, PORTING_LEDGER entries for cytoscape + react-cytoscapejs marked `VENDORED`. Amended to add D7 (cold-boot degradation on list endpoints returns 200 empty when MemoryPort not booted; node-detail still 503) | **Ratified v25 (2026-08-01)** | Stage 1.5 Wave D |

| ADR-071 | `ADR-071-stage-1-5-wave-e-polish.md` | Stage 1.5 Wave E · Post-Realization Polish — closes three deferred items from Wave D: (D1) new route `GET /api/gnosis/graph/communities?corpus=` runs `networkx.algorithms.community.louvain_communities` with `seed=42`, returns `{algorithm, communities:{node_id:community_id}, modularity, corpus, computed_at, node_count, edge_count}`, degrades to empty-page 200 when MemoryPort not booted. (D2) new route `POST /api/gnosis/graph/annotate` accepts `{node_id, provenance, confidence, note, reason}` and calls `MemoryPort.write_event(predicate="annotation", ...)` with zero-trust validation at both request and port layers; returns 400 on zero-trust failure, 503 when memory not ready. (D3) kernel subscribes to `zetesis.research.completed` in Zetesis boot lifespan via `EventBusPort.subscribe`; background `_drain_zetesis_reports_task` appends payload dicts into `registry.zetesis_reports` deque, cancelled on shutdown; best-effort failure per ADR-058. (D4) `_graph_zetesis_reports` rewritten for the actual event payload shape (payload dict, not `ResearchReport`) — `zetesis_evidences` edges kept, `zetesis_cited_by` edges intentionally deferred (payload has no citations). (D5) MemoryIntegrityPanel: community coloring toggle with golden-ratio hue spacing `hsl(cid * 137.5 mod 360, 60%, 50%)`, modularity badge, no layout change. (D6) inspector annotation form (note/provenance/confidence/reason), visible only on memory-triple nodes, hidden on `zetesis_report` nodes. (D7) `kernel/app.py` version 6.7.0 → 6.8.0; `_BootRegistry` gains `_zetesis_report_queue` and `_zetesis_drain_task`. Zero new ports; zero PORTING_LEDGER changes (`networkx>=3.4` already in `pyproject.toml`). ADR-007 preserved (event-bus-only). ADR-008/027 preserved (zero-trust write). ADR-057 preserved (kernel-owned routes). ADR-058 extended (subscriber added to boot). ADR-064 preserved (retrieval `/query` untouched). ADR-069 preserved (`/api/**` gate applies). ADR-070 amended (`_graph_zetesis_reports` corrected for payload shape). Rejects: client-side Louvain (non-deterministic), raw-triple `POST /api/gnosis/graph/edge` (bypasses port guard), direct plugin callback (ADR-007), polling `registry.zetesis_plugin.trials` (anti-pattern), extending event payload with citations (deferred). Lock-in condition: this ADR ratified, all pytest cases in `tests/kernel/test_stage_1_5_adr_071_wave_e.py` green on Colossus, Playwright `13-community-collapse-and-annotate.spec.ts` green, full Playwright suite ≥48/6/0, `kernel/app.py.version == "6.8.0"` | **Ratified v25 (2026-08-01)** | Stage 1.5 Wave E |
| ADR-072 | `ADR-072-stage-1-5-wave-f-panel-completion.md` | Stage 1.5 Wave F · GUI realization: F0 Tibetan Five-Wisdom theme (install `@tailwindcss/postcss` + `postcss`, add `ui/postcss.config.mjs`, expand `ui/app/globals.css` from 12→506 lines with Nagtang elevation ramp + directional hue-per-function panel borders + Ratnasambhava-gold hairline reserved for signed/ratified states — styled via `[data-testid]` attribute selectors, no component-file churn); F1 EventsWSProvider mounted once in `PersistentShell` subscribing to `/api/events/ws` with exponential 500 ms→8 s backoff, `useEventListener(type, cb)` API, default types `phrouros.anomaly.detected`/`zetesis.research.started`/`zetesis.research.completed`/`kernel.suspended`/`kernel.resumed`, `AgentTracePanel` + `ApprovalsQueuePanel` wired; F2 four kernel-authoritative Operate panels replace PlaceholderPanel (`StubDegradationPanel`→`/api/kernel/schema`, `ModelSwapSLOPanel`→`/api/ollama/status` 5 s poll, `ContextPressurePanel`→`/api/resources/balances` 15 s + `kernel.resumed`, `HardwareResiliencePanel`→`/health` + `/api/ollama/status` 10 s), rendered unconditionally by `PanelGrid` matching MEMORY_INTEGRITY/AGENT_TRACE pattern. Also folds `ui/tsconfig.json` Next 16 regenerated shape. F3+F4+F5 in PR #19 (MemoryIntegrity search+stats, NotificationTray, `/kernel` introspection page). Kernel version held at 6.8.0 through both code PRs; ratification PR flips ADR-072 Proposed→Ratified v25 and bumps 6.8.0→6.9.0. Rejects: (A) className rewrite of every component (thrashes F1/F2 review + breaks Playwright anchors), (B) per-panel WebSocket subscriptions (N sockets, no central backoff), (C) waiting for plugin descriptors to register Operate panels reading kernel routes (inverts ownership). Ratification PR (2026-08-01) also folds in §D test hardening (kill-switch file-level `afterAll` resume; single-retry on the two Zetesis SSE specs) and §E Next.js chain-CVE mitigation — escalated from initial 16.0.7 target to current Active LTS `next 16.2.11 + react/react-dom 19.2.4` after pnpm flagged 16.0.7 still deprecated for the 2025-12-11 follow-on CVEs; single bump covers CVE-2025-66478 through CVE-2026-64649. Lock-in DoD MET on Colossus at `714f7a6`: full Playwright **68/7/0** across two consecutive runs, Zetesis fast tier **78/78**, `kernel/app.py` version `6.9.0`. | **Ratified v25** · 2026-08-01 | Stage 1.5 Wave F |
| ADR-073 | `ADR-073-embeddings-port.md` | Stage 1.6 Phase 0 · EmbeddingsPort split — lifts `embed()` off `LLMPort` into a dedicated `ports/embeddings.py` with primary `OllamaEmbeddingsAdapter` calling Ollama's native `/api/embed` (default model `nomic-embed-text`, 768-dim). Locks five decisions: (D1) new `EmbeddingsPort` `Protocol` with batch-only `embed(texts: list[str])`, `dimensions(model=None)`, `is_healthy()`, idempotent `close()`. (D2) `adapters/embeddings/ollama/adapter.py` — native `/api/embed` endpoint (NOT `/v1/embeddings` compat), no fake api_key required, uses `httpx.AsyncClient`. (D3) Deprecate `LLMPort.embed()` — `warnings.warn(DeprecationWarning)` in `OllamaAdapter.embed()` and `LlamaSwapAdapter.embed()`, retained through deprecation window to preserve Stage 6.3.9 factory (ADR-054) parity; hard delete is a future ADR. (D4) `adapters/memory/dozerdb/graphiti_temporal_index.py` constructor accepts `embeddings: EmbeddingsPort | None = None`; when provided, wraps in `KosmosEmbedderAdapter` and passes to `Graphiti(embedder=...)` — deletes the `OpenAIEmbedderConfig(api_key="ollama-not-used", ...)` inline shim. `OpenAIRerankerClient` unchanged pending future cross-encoder port. (D5) `kernel/app.py` version `6.9.0 → 6.10.0`; `EmbeddingsPort` wired into boot registry alongside `LLMPort`. Rejects: (A) delete `LLMPort.embed()` in one shot (ripples into Stage 6.3.9 factory), (B) extend `LLMPort` with typed `embed_batch()` (still forces chat-only backends to implement embeddings surface), (C) fold embeddings into `VectorPort` (re-couples compute + storage that ADR-026 split). Zero new vendored code (Ollama native `/api/embed` reached via `httpx`, already a satisfied port). Test contract: `adapters/embeddings/ollama/test_contract.py` (live-Ollama, skip if unreachable) + `tests/ports/test_embeddings_protocol.py` (stub conformance). Stage 1.6 Phase 1 semantic-memory work MAY NOT begin until ADR-073 Ratified v25. Lock-in condition: this ADR ratified, `ports/embeddings.py` + `adapters/embeddings/ollama/adapter.py` land, deprecation warnings on `LLMPort.embed()` in place, Graphiti wiring migrated with deprecation window intact, `kernel/app.py.version == "6.10.0"`. | **Ratified v25** · 2026-08-01 | Stage 1.6 Phase 0 |
| ADR-074 | `ADR-074-semantic-memory-and-graph-visualization.md` | **Ratified v25 (2026-08-01)** · Stage 1.6 Phase 1 · Semantic memory surface + 2D/3D graph visualization port — extends `MemoryPort` with `search_semantic(query, corpus, limit, min_score)`; boots `registry.vector` (QdrantVectorAdapter) alongside `registry.embeddings`; adds `SemanticMemoryPath` helper composing EmbeddingsPort + VectorPort inside the DozerDB memory adapter; swaps Zetesis embed path to `EmbeddingsPort` (removing last `LLMPort.embed()` call site); ports Rigpa-LMS (Apache-2.0) `DimensionalForceGraph` + `graphDimensionStore` + `GraphDimensionToggle` into `ui/`, vendoring `react-force-graph-2d`, `react-force-graph-3d`, `three`; extends `ui/app/gnosis/page.tsx` with a 2D/3D force-graph section reading existing `/api/gnosis/graph/{nodes,edges}` routes; kernel version `6.10.0 → 6.11.0`. Rejects: (A) semantic memory as a separate plugin (violates ADR-007 or duplicates zero-trust guard), (B) hand-built d3-force + Three.js (multi-week critical-path work with an Apache-2.0 solution already proven), (C) deferring Zetesis embed swap (drift risk during ADR-073 deprecation window), (D) tightening `min_score` default (per-corpus calibration deferred). | **Ratified v25** · 2026-08-01 | Stage 1.6 Phase 1 |
| ADR-075 | `ADR-075-stage-1-6-phase-2.md` | **Ratified v25 (2026-08-01)** · Stage 1.6 Phase 2 · Semantic memory query surface + graph pagination + Zetesis fan-out + Graphiti hard-delete. D1 hard-deletes `graphiti_temporal_index.py` + `kosmos_graphiti_embedder.py` + contract tests (retires ADR-073 §4 deprecation window — supersedes that clause). D2 adds `POST /api/memory/search-semantic` + new `/memory/search` UI page (corpus filter, hit list) parallel to `/gnosis/graph` shape. D3 subscribes a kernel drain to `zetesis.research.completed` that calls `MemoryPort.write_event(kind="zetesis.report", ...)` — `SemanticMemoryPath` fan-out handles embed+upsert; preserves ADR-007. D4 client-side `next_cursor` pagination in `ui/app/gnosis/graph/page.tsx` with `MAX_PAGES=10` ceiling. D5 kernel version `6.11.0 → 6.12.0` + updates the 6.11.0 assertion in `13-community-collapse-and-annotate.spec.ts`. Rejects: (A) amend ADR-073 in place (mutates a Ratified v25 record), (B) skip D1 (ValidationError pollutes DEBUG_LOG), (C) split into four ADRs (same shape as ADR-074), (D) inline Zetesis fan-out (violates ADR-007), (E) uncapped pagination (community-collapse is the correct answer at scale per ADR-070 D3). | **Ratified v25** · 2026-08-01 | Stage 1.6 Phase 2 |
| ADR-076 | `ADR-076-stage-1-6-phase-3.md` | **Ratified v25 (2026-08-01)** · Stage 1.6 Phase 3 · Semantic-search real-Qdrant DoD + quarantine surface + provenance surface + AMG UI. D1 env-gated live-tier pytest fixture (`KOSMOS_STAGE_16_LIVE=1`) asserts real semantic hits against running Qdrant (matches ADR-049 Stage 4.4 pattern; rejects Docker Compose as premature-Stage-21 ops surface). D2 `/memory/search` polish — result highlighting, all-corpora facet, empty-state, distinct error surface. D3 Playwright live-tier Zetesis→semantic round-trip test asserting `provenance="zetesis.event_bus"` + `confidence=1.0`. D4 `MemoryPort.list_quarantined`/`approve_quarantined`/`reject_quarantined` port extension + `/api/memory/quarantined` routes + `/memory/quarantine` UI reading `/api/kernel/identity` for reviewer (ADR-069). D5 `/api/memory/provenance/{event_id}` route walking `:PROVENANCE_OF` edges with `MAX_DEPTH=10` + `/memory/provenance/[event_id]` UI, deep-linked from search hits. D6 `/api/memory/amg/status` route surfacing real `agent-memory-guard==0.3.0` registry (`AmgGuardPolicy` accessors) + verdict counters + quarantine count + AMG status pill on `/memory` header (satisfies spec §121 standing action). D7 kernel version `6.12.0 → 6.13.0` + `PORT_CONTRACTS.md` MemoryPort `ui_parity_status: partial → full`. Rejects: (A) Docker Compose fixture for D1 (Stage 21 ops surface), (B) skip D1 (leaves ADR-074 D3 promise unobserved), (C) fold quarantine/provenance UI into `/gnosis` (memory-plugin ownership boundary), (D) hard-coded AMG detectors (surfaces documentation, not operational truth), (E) split D1 into own ADR (D3 depends on it), (F) skip D4 (quarantine writes accumulate with no operator visibility), (G) amend ADR-075 (mixes Colossus verify gates). Rigpa-LMS donor code inspected at `backend/src/rigpa/domains/memory/` — no quarantine/provenance/AMG surface exists there; Phase 3 code is greenfield behind formal MemoryPort protocol extensions. Deep-links from `/memory/search` hit rows to `/memory/provenance/[event_id]` mirror Stage 4.6 gate UX. | **Ratified v25** · 2026-08-01 | Stage 1.6 Phase 3 |

| ADR-077 | `ADR-077-kosmos-lms-integration-cut.md` | **Ratified (2026-09-10)** · Kosmos-LMS integration cut — create `rmholston420/kosmos-lms` (public, MIT) as full fork of `rmholston420/kosmos`; absorb `rmholston420/tektos-ultima` as **the** implementation of the `plugins/tektos/` scaffold (preserving ADR-036/037/038/041/042/044/045/046/063/065). Six new formal ports (Immune ADR-079, LoopSafety ADR-080, Thermal ADR-081, Sandbox ADR-082, Voice ADR-083, Vision ADR-084); MemoryPort extension (ADR-085 `search_hybrid`); EventBusPort envelope taxonomy (ADR-086); Hermes LLM topology adapter (ADR-087); loop-safety read-only budget interlock (ADR-088); FrontendContractPort `PanelKind.IFRAME` (ADR-089); SelfModificationPort proposed but deferred (ADR-090). MIT re-license at port-in by sole copyright holder (rmholston420). Kosmos Next.js shell hosts Tektos Next.js UI as iframe/microfrontend. Tektos-Ultima CI (ruff, mypy, pytest, next build, eslint, Playwright chromium) becomes kosmos-lms CI baseline. Rejects: (A) subtree-vendoring tektos-ultima under `plugins/tektos/vendor/` (defers port-adaptation work), (B) submodule (splits history + PR flow), (C) wholesale replacement of Kosmos `plugins/tektos/` scaffold (invalidates 10 ratified ADRs + 5,565 LOC exit-gate tests), (D) unified Next.js shell (forces Next 15→16 reconciliation + Monaco/xterm dependency merge on the critical path). | **Ratified** · 2026-09-10 | Stage 0 |
| ADR-078 | `ADR-078-kosmos-build-spec-v26-cut.md` | **Ratified (2026-09-10)** · Kosmos-Build-Spec v26 cut — archived v25 to `docs/archive/`; new v26 preserves §0–§24 numbering and the v25 Addendum, adds new **§25 Tektos absorption** (absorption principle, frontend strategy, hindsight H1→H2, provenance taxonomy, kernel boot order, read-only-budget interlock, reserved ports table). Renamed sequence to `Kosmos-Build-Sequence-v26.md` with new stages 3.13, 4.7, 5.6, 6.5, 7.4 inserted at natural DoD boundaries. | **Ratified** · 2026-09-10 | Stage 1.2 |
| ADR-079 | `ADR-079-immune-port.md` | **Ratified (2026-09-10)** · `ImmunePort` new formal port (16th) — `scan(request) → ImmuneVerdict{allow|warn|block}` + `register_detector` + `list_detectors`; every verdict publishes on `EventBusPort` (`immune.verdict.*`) and blocks write to `MemoryPort` with `provenance="immune_verdict"` `confidence=1.0`. Adapters under `adapters/immune/<vendor>/`; Tektos-Ultima donor adapter provides 12 detectors. Rejects fold into ApprovalPort (different life-cycles). | **Ratified** · 2026-09-10 | Stage 3.13 |
| ADR-080 | `ADR-080-loop-safety-port.md` | **Ratified (2026-09-10)** · `LoopSafetyPort` new formal port (17th) — `begin_turn` / `record_tokens` / `record_tool_call(read_only)` / `check_repetition` / `end_turn`; `LoopCaps{max_turns=15,max_tokens_total=65536,max_wall_time_seconds=300,repetition_window=3,read_only_budget=10}`; terminal states publish on `EventBusPort` (`loop_safety.*`) and write to `MemoryPort` (`provenance="loop_safety"`, `confidence=1.0`). Read-only-budget interlock formalised by ADR-088. Rejects fold into ResourcePort (different life-cycles). | **Ratified** · 2026-09-10 | Stage 3.13 |
| ADR-081 | `ADR-081-thermal-port.md` | **Ratified (2026-09-10)** · `ThermalPort` new formal port (18th) — `sample()` async + `pressure()` sync-non-throwing + `apply_power_cap` / `release_power_cap`; Colossus RTX 5090 thresholds yellow 51 °C / cap 80 °C / red 88 °C, 400 W GPU cap. Threshold-crossing publishes on `EventBusPort` (`thermal.<level>`) and red-state writes to `MemoryPort` (`provenance="thermal"`, `confidence=1.0`). `LLMPort` adapters consult `pressure()` and refuse inference during red state. Rejects fold into ResourcePort. | **Ratified** · 2026-09-10 | Stage 3.13 |
| ADR-082 | `ADR-082-sandbox-port.md` | **Ratified (2026-09-10)** · `SandboxPort` new formal port (19th) — `run(SandboxRequest) → SandboxResult` with `SandboxLimits{max_wall_seconds,max_memory_mb,max_cpu_percent,network="none"}`; Linux namespaces + cgroups v2 primary adapter (Tektos donor); `NoOpSandboxAdapter` for CI. Every result publishes on `EventBusPort` (`sandbox.*`) and writes to `MemoryPort` (`provenance="sandbox"`, `confidence=1.0`). Network defaults fail-closed. Rejects generic ExecPort. | **Ratified** · 2026-09-10 | Stage 4.7 |
| ADR-083 | `ADR-083-voice-port.md` | **Ratified (2026-09-10)** · `VoicePort` new formal port (20th) — `transcribe(audio) → Transcript` + `synthesize(text) → AudioClip` + `list_voices`; `Transcript.confidence` passthrough on `MemoryPort` writes (`provenance="tektos_frontend"`). Adapter selection deferred to Stage 6.5 (candidates Whisper.cpp + Piper primary). Rejects split into STTPort/TTSPort (adapters share code). | **Ratified** · 2026-09-10 | Stage 6.5 |
| ADR-084 | `ADR-084-vision-port.md` | **Ratified (2026-09-10)** · `VisionPort` new formal port (21st) — `describe(image)` + `extract_text(image)` → OCRResult + `detect(image,categories)`; image bytes land in `adapters/data/` blob store (content-addressed hash), `MemoryPort` write carries hash + result with confidence passthrough. Adapter selection deferred to Stage 6.5 (candidates LLaVA / Qwen2-VL / Tesseract). Rejects generic MultimodalPort. | **Ratified** · 2026-09-10 | Stage 6.5 |
| ADR-085 | `ADR-085-memoryport-search-hybrid.md` | **Ratified (2026-09-10)** · `MemoryPort` surface extension — `search_hybrid(query, corpus, limit, lexical_weight, semantic_weight, min_score) → tuple[MemoryHit,...]` using RRF (k=60) by default; weights sum to 1.0 (port-level guard); adapters without lexical index raise NotImplementedError (no silent degrade). Serves Tektos post-hindsight-migration (Stage 7.4). Amends ADR-027 (extends, does not remove existing methods). | **Ratified** · 2026-09-10 | Stage 1.3 surface / Stage 7.4 adapter |
| ADR-086 | `ADR-086-eventbus-envelope-taxonomy.md` | **Ratified (2026-09-10)** · `EventBusPort` envelope-kind namespace locked for Tektos-side subsystems — reserved prefixes `immune.*`, `loop_safety.*`, `thermal.*`, `sandbox.*`, `hindsight.*`, `tektos.*` with enumerated envelope kinds per subsystem. `producer_plugin` still required per ADR-023 rule 2. CI lint (Stage 0.7) flags unlisted namespaces from Tektos-side code. Amends ADR-023 (locks the namespace ADR-023 left open). | **Ratified** · 2026-09-10 | Stage 3.13 |
| ADR-087 | `ADR-087-hermes-llm-topology.md` | **Ratified (2026-09-10)** · Hermes topology adapter for `LLMPort` — role-aware routing (`role=planner\|coder\|reflection\|default`) with CPU planner + GPU coder split via localhost failover endpoint; failover publishes `llm.role.failover` envelope; thermal-red halts inference (ADR-081). Opt-in via `IDENTITY.toml` `llm.primary_adapter="hermes"`; respects ADR-057 Rule 1 (llama-swap-only for GPU side). CPU planner candidate benchmark locked Stage 4.7. Rejects role parameter on LLMPort Protocol; rejects separate PlannerLLMPort/CoderLLMPort. | **Ratified** · 2026-09-10 | Stage 4.7 |
| ADR-088 | `ADR-088-loop-safety-read-only-budget.md` | **Ratified (2026-09-10)** · Loop-safety read-only tool-call budget interlock formalised inside `LoopSafetyPort` — default 10 read-only calls per turn (spec §25.7); exhaustion returns `status="budget_exhausted"` and forces text-only turn completion; publishes `loop_safety.read_only_budget_exhausted` envelope. Non-read-only calls do not consume budget; classification is tool-registry's responsibility (Stage 4.7). Non-bypass rule enforced at port layer. Amends ADR-080 (this locks the record_tool_call contract). | **Ratified** · 2026-09-10 | Stage 3.13 |
| ADR-089 | `ADR-089-frontend-contract-panel-iframe.md` | **Ratified (2026-09-10)** · `FrontendContractPort` extended with `PanelKind.IFRAME` — iframe panels carry `src`, `sandbox` HTML attrs (default `allow-same-origin`, `allow-scripts`, `allow-forms`), `title`, `size_hint`; same-origin via reverse proxy; postMessage bridge with origin validation re-publishes to server-side `EventBusPort`; CSP `frame-ancestors 'self'`. Enables Kosmos Next 16.2.11 shell to host Tektos Next 15.4 UI as microfrontend under `/tektos/frontend`. Rejects separate origin; rejects unified Next.js shell (Next 15→16 reconciliation on critical path). | **Ratified** · 2026-09-10 | Stage 2 |
| ADR-091 | `ADR-091-microfrontend-shell-integration.md` | **Ratified (2026-09-10)** · Operationalises ADR-089 for the first absorbed microfrontend: Tektos-Ultima UI lands at **`/tektos-ultima`** (preserves existing ADR-065 `/tektos` approval list). Kernel exposes `/tektos-ultima/frontend/*` via Starlette streaming proxy to `KOSMOS_TEKTOS_ULTIMA_UPSTREAM` (default `http://127.0.0.1:5556`, spec §25.7). Panel is single `PanelKind.IFRAME` with `DEFAULT_IFRAME_SANDBOX`. Bridge: `POST /api/tektos-ultima/bridge` accepts envelopes, enforces `source="tektos-ultima"` + `tektos.*` namespace (ADR-086), publishes to `EventBusPort`. Client bridge validates `event.source === iframe.contentWindow` and `event.origin === window.location.origin`. CSP `frame-ancestors 'self'`. Rejects `/tektos` displacement; rejects cross-origin iframe; rejects direct Tektos component import into Kosmos `ui/`. | **Ratified** · 2026-09-10 | Stage 2 |
| ADR-092 | `ADR-092-tektos-runtime-absorption-scope.md` | **Ratified (2026-09-10)** · Locks the Stage 3.13 scope for the Tektos runtime absorption. Vendors three donor snapshots (loop_safety, immune, thermal metrics) into `adapters/*/tektos/vendor/`; wraps them behind the Stage 1 formal ports `LoopSafetyPort` (ADR-080, adds ADR-088 read-only budget), `ImmunePort` (ADR-079, 3 seed detectors), `ThermalPort` (ADR-081, adds level classification); lands a minimal `plugins/tektos/runtime/turn_loop.py` composing the three adapters through their ports + `EventBusPort` (`tektos.agent.turn.*` envelopes). Explicit exclusions: `ImmuneSystem` orchestrator, 9 remaining detectors, `ThermalRegulator` PID loop, `loop_guard.py`, and 25+ other `runtime/` siblings (LLM inference, planner, sandbox, MCP, RAG, self-modification) — all deferred to Stage 4.7+. Rejects wholesale-import of `orchestrator/` (violates ADR-007) and hand-rewrite (violates kosmos-port-workflow). | **Ratified** · 2026-09-10 | Stage 3 |
| ADR-094 | `ADR-094-tektos-tool-surface-reconciliation-and-filesystem-tools.md` | **Ratified (2026-09-10)** · Reconciles the two tool-execution surfaces that co-existed after Stage 4.7 (Stage-3.2 `TektosAgent.call_tool` MCP path and Stage-4.7 `TektosToolRegistry.invoke` sandbox path) onto a single substrate. Ships four filesystem tools (`file_read`/`file_list`=AUTONOMOUS, `file_write`=HUMAN_REVIEW, `file_delete`=HUMAN_REQUIRED) through `SandboxPort.run` with argv-only execution and `network="none"`. Ships `PathTraversalDetector` implementing the `Detector` protocol from `ports/immune.py`, registered as pre-approval detector so `..`, absolute escapes, and symlink escapes never reach `ApprovalPort.propose`. Ships `MCPToolBridge` translating `MCPPort.list_tools()` → `ToolDescriptor` registrations preserving the locked `TEKTOS_TOOL_TIER_MAP` (ADR-037) with fail-closed `HUMAN_REQUIRED` default. `TektosAgent.call_tool` becomes a thin adapter delegating to `tool_registry.invoke` when constructed with `tool_registry=...`, preserving Stage-3.2 inline flow otherwise. Enforces ADR-079 rules 1+2 on detector-blocked calls (publishes `immune.verdict.block` envelope + writes `MemoryPort` with `provenance='immune_verdict'` and `confidence=1.0`). | **Ratified** · 2026-09-10 | Stage 4.8 |
| ADR-093 | `ADR-093-tektos-sandbox-planner-tools-absorption-scope.md` | **Ratified (2026-09-10)** · Locks the Stage 4.7 scope for the Tektos sandbox + planner + tool-registry absorption. Vendors two donor snapshots (`sandbox_exec_donor.py` trimmed from `providers/sandbox_provider.py` 763→180 lines; `tool_registry_donor.py` trimmed from `tools/registry.py` 553→140 lines) under `adapters/sandbox/tektos/vendor/`. Lands two SandboxPort adapters: `TektosSandboxAdapter` (argv-only exec, `resource.setrlimit` via `preexec_fn`, `unshare --user --map-root-user --net` for `network="none"`, fail-closed `sandbox.isolation_unavailable` envelope when unavailable, opportunistic cgroups v2 write, wall-time enforcement, lifecycle envelopes, MemoryPort writes) and `NoOpSandboxAdapter` (synthetic result for CI; required by ADR-082 rule 4). Lands a Kosmos-native `TektosTurnPlanner` seed emitting `tektos.plan.started`/`node`/`completed` on `EventBusPort` (no LLM at 4.7). Lands `TektosToolRegistry` with `ToolDescriptor(approval_tier, network)` gating every invoke through `ApprovalGatewayPort.propose` + polling `ApprovalResolverPort.get_by_id` for HUMAN_REVIEW/HUMAN_REQUIRED tiers; execution routes through `SandboxPort.run`; every `tektos.tool.{invoked,approved,denied,completed}` payload carries `provenance="tektos_tool"` + `confidence=1.0`. Explicit exclusions: LLM-driven planner (`translator`/`disambiguator`/`spec_generator`/`orchestrator`/`template_selector`), file-system tools, `search` tool, MCP tool discovery, Docker-exec proxy, cgroups v1 fallback, Firecracker/gVisor VM isolation, real Praxis APEX wiring — all deferred to Stage 4.7+1 through 4.8+ and captured as PLANNED in PORTING_LEDGER. | **Ratified** · 2026-09-10 | Stage 4.7 |
| ADR-095 | `ADR-095-stage-5-6-self-improvement-self-repair-propose-only-scope.md` | **Ratified (2026-09-10)** · Locks Stage 5.6 propose-only scope: **D1** vendor donor data-model primitives only (`RepairStatus`, `RepairStrategy`, `DegradationLevel`, `RepairRecord`, `ExperienceRecord`) under `adapters/tektos/vendor/`; donor engines (`self_repair/{engine,strategies,workflows,health_monitor,effectiveness}.py`, `self_improvement/engine.py`, `agents/self_improvement/loop_orchestrator.py`, `self_modification/self_{gui,test}_expander.py`) are intentionally NOT ported. **D2** two Kosmos-native proposers land at `plugins/tektos/self_improve/proposer.py` (`SelfImprovementProposer`) + `plugins/tektos/self_repair/proposer.py` (`SelfRepairProposer`) each exposing `propose()` (routes through `ApprovalGatewayPort.propose` + `MemoryPort.write_event` with `provenance='tektos_self_modification'` + `confidence=0.85` + `EventBusPort.publish` on `tektos.self_modification.proposed` reserved by ADR-086) and `apply()` (raises `NotImplementedError` referencing ADR-090). **D3** approval tier locked to `HUMAN_REQUIRED` (no `HUMAN_REVIEW`, no `AUTONOMOUS`). **D4** deny path writes second `MemoryPort` triple with `predicate='tektos.self_modification.denied'` so denials are as observable as proposals. **D5** no `ports/self_modification.py` file lands in Stage 5.6 — proposers consume `ApprovalGatewayPort` + `MemoryPort` + `EventBusPort` directly; refactored to `SelfModificationPort` after ADR-090 ratifies. | **Ratified** · 2026-09-10 | Stage 5.6 |
| ADR-096 | `ADR-096-stage-6-5-voice-vision-scope-and-two-write-pattern.md` | **Ratified (2026-09-10)** · Locks Stage 6.5 scope: `VoicePort`/`VisionPort` **adapters** land (surfaces were already locked at Stage 1 by ADR-083/084). **D1** two-write MemoryPort pattern for `tektos_frontend` provenance — ingest triple `confidence=1.0` (matches spec §25.4) + result triple `confidence=<result.confidence>` (matches ADR-083/084 passthrough). **D2** content-addressed ingest URI `kosmos://blob/<sha256>`. **D3** `BlobStore` helper at `adapters/data/blobs/` (sha256-keyed, NOT a formal port). **D4** Stage 6.5 lands 2 voice adapters (NoOp + faster-whisper) + 3 vision adapters (NoOp + Ollama-Qwen2.5-VL + Tesseract) + `TektosFrontendMemoryWriter`. **D5** no ApprovalGatewayPort routing (all verbs are read-only w.r.t. user state). Explicit exclusions: TTS engine selection (deferred to Stage 6.5+1 ADR), VoicePort streaming, wake-word + VAD orchestration, frontend UI wiring (Stage 6.6). | **Ratified** · 2026-09-10 | Stage 6.5 |
| ADR-097 | `ADR-097-voice-port-adapter-selection-faster-whisper-stt-tts-deferred.md` | **Ratified (2026-09-10)** · VoicePort adapter selection. **D1** faster-whisper (MIT, SYSTRAN-maintained) as Stage 6.5 STT adapter; env-tunable (`KOSMOS_WHISPER_MODEL`/`KOSMOS_WHISPER_DEVICE`/`KOSMOS_WHISPER_COMPUTE_TYPE`). **D2** vendor donor behaviour — not code (rewrite behind port smaller than wrapper). **D3** TTS engine selection deferred to Stage 6.5+1 ADR after benchmark: Piper `OHF-Voice/piper1-gpl` is GPL-3.0 (viralizes monorepo — rejected per `kosmos-port-workflow` §3), Coqui shuttered late 2023 (MPL-2.0 forks like `idiap/coqui-ai-TTS` require standalone benchmark), `edge-tts` is Microsoft cloud (violates self-hosted preference — rejected). Interim: `FasterWhisperVoiceAdapter.synthesize` raises `TTSNotConfigured`; `NoOpVoiceAdapter` returns 100 ms silent WAV for CI. **D4** NoOpVoiceAdapter shape locked. **D5** adapter dir layout locked (`adapters/voice/{noop,faster_whisper}/`). Post-Stage 6.5 TTS candidates: idiap/coqui-ai-TTS (MPL-2.0), StyleTTS2 (MIT), Kokoro-82M (Apache-2.0). | **Ratified** · 2026-09-10 | Stage 6.5 |
| ADR-099 | `ADR-099-stage-7-4-rescope-h1-skipped-search-hybrid-lands.md` | **Ratified (2026-09-10)** · Stage 7.4 re-scope. **D1** Retire H1 detour on paper only — `hindsight_bridge` ledger row flips `PLANNED (Stage 3-5)` → `EVALUATED-REJECTED (Stage 3-5, H1 skipped)`; no repo state changes because H1 was never built. **D2** Land `MemoryPort.search_hybrid` on `DozerDbMemoryAdapter` per ADR-085 verbatim: RRF fusion with `RRF_K = 60`, formula `score(h) = lw · rrf(rank_lex(h)) + sw · rrf(rank_sem(h))`, `validate_hybrid_weights` guard first (non-bypassable), merge by hit.id with semantic payload winning on collision, `min_score` filters on fused score. **D3** `LexicalIndex` is an adapter-scoped Protocol (NOT a formal port under `ports/`) — symmetric with `GraphBackend`/`TemporalIndex`; ADR-007 + ADR-027 forbid plugin bypass of `MemoryPort`. **D4** Ship `InMemoryLexicalIndex` (BM25-Okapi, k1=1.5, b=0.75) now; defer real `DozerDbLexicalIndex` (Neo4j Lucene fulltext via `CALL db.index.fulltext.queryNodes`) to Stage 7.4+1. **D5** No `ports/memory.py` changes — ADR-085 added the surface at Stage 1.3. **D6** 11 new contract tests land + the 6 pre-existing MemoryPort protocol-drift failures resolve automatically → first Kosmos stage with zero pre-existing failures (**1437 passed / 0 failed / 14 skipped**). Rejects: extending `ports/memory.py` for `LexicalIndex` (would violate ADR-007 by exposing an internal seam to plugins); silent semantic-only degrade when lexical unwired (violates ADR-085 honesty rule); tying Stage 7.4 to a live DozerDB Lucene setup (adds Docker dependency to the CI critical path). | **Ratified** · 2026-09-10 | Stage 7.4 |
| ADR-098 | `ADR-098-vision-port-adapter-selection-qwen-vl-ollama-tesseract.md` | **Ratified (2026-09-10)** · VisionPort adapter selection. **D1** ships 3 adapters: `OllamaQwenVLVisionAdapter` (`describe` + `detect` via `qwen2.5-vl:7b` at `http://127.0.0.1:11434`, Apache-2.0) + `TesseractVisionAdapter` (`extract_text` via pytesseract, Apache-2.0) + `NoOpVisionAdapter`. **D2** split adapters, not composite — each adapter raises `VisionCapabilityUnsupported` on verbs it does not implement (different failure modes deserve different `is_healthy()` reports). **D3** OCR routing: Qwen2.5-VL can OCR but returns free-text — port contract requires block-level bboxes + calibrated confidences, which Tesseract provides natively. **D4** confidence sources: Qwen2.5-VL emits no confidence → `0.5` calibrated placeholder for `describe`, passthrough on detect JSON `confidence` field (default `0.5`); Tesseract aggregate = mean of block confidences (blocks with `conf=-1` excluded). **D5** adapter dir layout locked. Rejected: Llama 3.2 Vision (Meta community license), cloud vision APIs, LLaVA as primary (weaker OCR at same VRAM). LLaVA remains fallback via `KOSMOS_VISION_MODEL=llava:7b`. | **Ratified** · 2026-09-10 | Stage 6.5 |
| ADR-100 | `ADR-100-dozerdb-lexical-index-neo4j-fulltext.md` | **Ratified (2026-09-10)** · Stage 7.4+1 lands the production `DozerDbLexicalIndex` backing the `LexicalIndex` Protocol from ADR-099 D3. **D1** adapter shape mirrors `DozerDbGraphBackend` (lazy `neo4j.AsyncGraphDatabase`; per-call `AsyncSession`; `_init_error` capture; sync + non-throwing `is_healthy`; idempotent async `close` swallowing driver errors into a warning). **D2** lazy bootstrap on first `index_event`/`search_lexical` via `CREATE FULLTEXT INDEX $index_name IF NOT EXISTS FOR (n:MemoryEvent) ON EACH [n.text]`; `label` + `index_name` are constructor-tunable, both validated by the `DozerDbGraphBackend` identifier guard (`^[A-Za-z_][A-Za-z0-9_]*$`) before literal Cypher interpolation because `CREATE FULLTEXT INDEX` does not accept `$name` parameter substitution. **D3** single-property text index; `index_event` writes `MERGE (n:MemoryEvent {id: $id}) SET n.text = $text, n.as_of = datetime($as_of_iso), n.corpus_name = $corpus`, with `text` from the shared `_lex_text_from_payload` extractor (parity with `InMemoryLexicalIndex`). **D4** `search_lexical` runs `CALL db.index.fulltext.queryNodes($index_name, $query) YIELD node, score` with a post-YIELD `WHERE $corpus IS NULL OR node.corpus_name = $corpus` filter and returns `MemoryHit`s with minimal payload rehydration (`{"text": ..., "attributes": {"corpus_name": ...}}` — full triple via `MemoryPort.query_temporal`); Lucene procedure failures degrade to `[]` + warning log. **D5** two test tiers: fast (mocked driver, 25 tests) + env-gated live (`KOSMOS_STAGE_74_REAL_DOZERDB_LEXICAL=1` against `ops/compose/memory.yml`). **D6** ledger flip PLANNED→VENDORED; `LexicalIndex` Protocol shape unchanged; boot-time wiring into `ZetesisPlugin` factory deferred to Stage 7.4+2 (own ADR). Rejects: SQLite FTS5 backend (new dependency), Elasticsearch (JVM footprint), boot-wiring in this slice (factory-contract co-ownership), multi-corpus multi-index design (does not scale), multi-property fulltext index (field-boost quirks would diverge from `InMemoryLexicalIndex`). Full regression: **1462 passed / 0 failed / 15 skipped**. | **Ratified** · 2026-09-10 | Stage 7.4+1 |
| ADR-101 | `ADR-101-stage-7-4-2-kernel-boot-lexical-wiring.md` | **Ratified (2026-09-10)** · Stage 7.4+2 wires `DozerDbLexicalIndex` at kernel boot; discharges ADR-100 D6 deferral. **D1** wiring lives in `kernel/app.py::_boot_memory` (single canonical `DozerDbMemoryAdapter` construction site per ADR-063) — not in `plugins/zetesis/adapters/real/factory.py`; the factory signature stays unchanged and Zetesis inherits the wired lane through the existing `memory=registry.memory` seam. **D2** opt-in via new env var `KOSMOS_MEMORY_LEXICAL={off,dozerdb}` (default `off`); `dozerdb` requires `KOSMOS_MEMORY_BACKEND=dozerdb` (shared Bolt endpoint per ADR-008 single-canonical-store rule) — mismatched shape raises `RuntimeError` caught by outer `_try("memory")`; unknown value raises `RuntimeError` enumerating allowed values. **D3** boot-time `is_healthy()` check on the constructed index; unhealthy falls through to `lexical=None` with a warning log citing ADR-101 D3, so `search_hybrid` reverts to `NotImplementedError` per ADR-085; construction failures inside the wiring helper follow the same D3 fall-through path. **D4** wiring failure records into `registry.errors["memory"]` via the outer `_try("memory")` — no new `registry.errors["memory_lexical"]` subsystem key (contract-preserving degrade to `NotImplementedError` is not a kernel error state). **D5** six fast-tier tests at `tests/kernel/test_stage_7_4_2_lexical_wiring.py` cover unset, explicit `off`, reject-shape, healthy wire, monkeypatched-failing-driver fall-through, and unknown value; live tier reuses `KOSMOS_STAGE_74_REAL_DOZERDB_LEXICAL=1` from ADR-100 (no duplicate live smoke). Rejects: adding `lexical=` to the factory signature (two env-var-parsing sites; breaks/mutates existing test harnesses); auto-wire when `KOSMOS_MEMORY_BACKEND=dozerdb` (Neo4j configs may disable Lucene procs — auto-wire would boot into unusable state whose failure mode is worse than honest `NotImplementedError`); new `KOSMOS_DOZERDB_LEXICAL_URI` split endpoint (ADR-008 single-canonical-store forbids); auto-wire `InMemoryLexicalIndex` on the in-memory branch (risks shipping the test backend into production); lazy lexical bootstrap on first query (hides DNS/auth failures until first `search_hybrid`). DoD: 6 acceptance tests green; baseline **1462 passed / 0 failed / 15 skipped** PRESERVED (`tests/kernel/` is a pre-existing orphan outside `pyproject.toml testpaths` — logged in DEBUG_LOG for future cleanup). No `PORTING_LEDGER.md` changes (wiring-only slice). | **Ratified** · 2026-09-10 | Stage 7.4+2 |
| ADR-102 | `ADR-102-relational-memory-port-postgres-5th-layer.md` | **Ratified (2026-09-10)** · `RelationalMemoryPort` — new formal port (23rd). Postgres 18 + pgvector 0.8.1 + pg_uuidv7 slots as **5th memory layer** combining R1 (audit ledger: sessions/approvals/tool-invocation/ADR-088 budget history/ADR-092 immune events) + R2 (episodic-narrative store with tsvector + hnsw pgvector hybrid search). R3 (pgvector as EmbeddingsPort alternative) deferred to future ADR. Surface: `transaction`, `record_event`, `query_events`, `write_narrative`, `search_narratives`, `is_healthy`, `close`. Zero-trust write contract (parity with ADR-008) — every write carries `provenance` + `confidence`. Two adapters: `NoOpRelationalMemoryAdapter` (aiosqlite in-memory, required for CI) + `PostgresRelationalMemoryAdapter` (asyncpg + pgvector; Alembic migrations). Kernel wiring via `KOSMOS_RELATIONAL_MEMORY={off,noop,postgres}` env-gate; unhealthy falls through to `None` with warning log (ADR-101 degrade pattern). No overlap with `MemoryPort.search_hybrid` (triples/events vs prose narratives). Rejects: extending MemoryPort (violates single-responsibility); folding R3 pgvector-as-embeddings into this port (would confuse two distinct concerns); SQLAlchemy Core dependency (asyncpg + raw SQL keeps adapter small); auto-migrate on boot (ADR-063 no-implicit-schema-changes). | **Ratified** · 2026-09-10 | Plan v2 Stage 8.0 |
| ADR-104 | `ADR-104-tektos-turn-loop-session-and-llm-integration.md` | **Ratified (2026-09-10)** · Stage 8.2 grows `plugins/tektos/runtime/turn_loop.py::TektosTurnLoop` (landed at Stage 3.13 per ADR-092 §3) with four new *optional* port collaborators: `SessionPort` (D2 — start/complete/fail/interrupt turn transitions per Stage 8.1 ADR-103), `LLMPort` (D3 — single non-streaming `generate` call), `SandboxPort` (D4 — per-`ToolCallSpec` execution when `.sandbox_request` provided), `ResourcePort` (D5 — post-turn best-effort `can_allocate(COMPUTE, 1)` check). New `TurnStopReason` literals `llm_error`, `sandbox_error`; new `TurnOutcome` fields `llm_response`, `resource_exhausted`, `error`; new `ToolCallSpec.sandbox_request`; new `ToolCallOutcome.sandbox_result`. Two new event types `tektos.agent.turn.llm_completed` + `tektos.agent.turn.sandbox_completed`; every published payload carries `session_id` when bound. New `TektosTurnLoop.interrupt(session_id)` mirrors donor `RuntimeSDK.interrupt`. Kernel `_boot_tektos_turn_loop` (env-gate `KOSMOS_TEKTOS_TURN_LOOP={off,on}`) lands in canonical order after `_boot_session`; unhealthy → `registry.tektos_turn_loop=None` with WARNING (ADR-101 degrade). `TektosPlugin` dataclass grows optional `turn_loop` field (D11). Kill-switch review (per ADR-103) rejects fidelity-porting donor `_stream_llm` (1471 lines, violates ADR-007/023/029 by construction); donor `HookRegistry` (325 lines) rejected wholesale (D7 — subsumed by SessionPort + EventBus). Explicit deferrals (D9): streaming LLM + multi-round agent loop (Stage 8.3+), tool-call routing from LLM to sandbox (Stage 8.3+), RAG/planner/hierarchical/multi-agent/repo-map/tool-router/task-decomposer/context-curator/context-compactor/LLM-failover (Stages 8.3–8.7 per Plan v2), per-session `asyncio.Lock` (Stage 8.3), pre-turn `ResourcePort.allocate` priority-queue enqueue (Stage 8.7 orchestrator scope). Rejects: rewriting from scratch as `TurnController` class (duplicates ~200 lines of Stage 3.13 immune/thermal/loop-safety wiring); making the four new ports required (breaks Stage 3.13 test surface); introducing a `HookPort` (unnecessary given EventBus subscription surface); streaming LLM at 8.2 (requires the whole 8.3–8.5 stack). No new formal port; no vendor port entry in PORTING_LEDGER (this is a port-consuming extension of a Kosmos-owned file). | **Ratified** · 2026-09-10 | Plan v2 Stage 8.2 |
| ADR-103 | `ADR-103-session-port-tektos-fidelity.md` | **Ratified (2026-09-10)** · `SessionPort` — new formal port (24th). Stage 8.1 lands the session-lifecycle FSM substrate that Stages 8.2–8.7 turn-loop / reflection / planner / executor / manager / multi-agent slices coordinate against. **D1** Protocol at `ports/session.py` with 22 methods (create_session, get/list_sessions, attach_client/detach_client (port-neutral rename of donor add_ws_connection/remove_ws_connection), start_turn/complete_turn/fail_turn/interrupt_turn, archive/fork/resume/rename/tag/delete/reap/search, get_state, get_history, get_allowed_transitions, is_healthy sync non-throwing, close async idempotent). Enum `SessionState` (CREATED, READY, RUNNING, INTERRUPTED, FAILED, IDLE, ARCHIVED — ARCHIVED added as first-class member vs donor raw-string). Frozen dataclasses `LiveSession` + `StateTransition`. Ten-transition table (7 donor + 3 new ARCHIVED transitions). **D2** Two adapters: `InMemorySessionAdapter` (asyncio.Lock-protected, CI-required) + `TektosSessionAdapter` fidelity port. Donor `state_machine.py` + `session.py` copied verbatim to `adapters/session/tektos/vendor/`; adapter façade rewrites three import boundaries (donor `tektos.event_bus.get_event_bus()` 3-arg positional publish → shim wraps into `EventEnvelope(producer_plugin='tektos.session', ...)` and delegates to injected `EventBusPort`; donor `tektos.store.event_store.append_event(...)` → shim publishes lifecycle envelope through same `EventBusPort` (SQLite event store deferred to Stage 13); donor cross-file `from tektos.state_machine import ...` retargeted to sibling vendored module). Optional `RelationalMemoryPort` mirror — when wired, every StateTransition mirrors to Stage 8.0 audit ledger with `provenance='tektos.session'`, `confidence=1.0`; mirror failures log-only, do not roll back FSM. **D3** No `EventStorePort` at Stage 8.1 — routes through `EventBusPort`; SQLite event store deferred to Stage 13. **D4** Kernel wiring `_boot_session` via env-gate `KOSMOS_SESSION={off,inmemory,tektos}` (default `off`); `tektos` requires `registry.event_bus`, optional `registry.relational_memory`; unhealthy → `registry.session=None` with warning log (ADR-101 degrade pattern). Boot order: `_boot_relational_memory` → `_boot_session` → `_boot_gnosis_seeder`. **D5** `TektosPlugin` dataclass unchanged at 8.1 (Stage 3.7 lock preserved); `session_port` integration deferred to Stage 8.2 with its own ADR. **D6** Fidelity-port rule enforced — donor modifications enumerated in PORTING_LEDGER Stage 8.1 entry; kill switch preserved (rewrite specific module against contract if invariant collision unresolvable with small mod). **D7** `SessionState` naming — port enum wins; donor `session_state.py::SessionState` dataclass (out of 8.1 scope) rename to `SessionSnapshot` recorded as future-stage obligation. Rejects: extending `EventBusPort` to accept 3-arg positional publish (violates ADR-023 envelope-first); rewriting FSM against fresh contract (discards donor PlexClaw bug-fix history documented in module docstring — bug #1 dead branch, bug #8 failed-session cleanup, bug #10 status-after-interrupt); reusing `MemoryPort` for lifecycle events (blurs zero-trust contract with high-frequency churn); new `EventStorePort` at 8.1 (over-scopes slice; defers Stage 13 authority); growing `TektosPlugin` at 8.1 (Stage 3.7 ADR-036 §Q4=B lock); keeping donor `add_ws_connection`/`remove_ws_connection` port names (transport-coupled naming leaks WebSocket assumptions); mandatory `RelationalMemoryPort` mirror (breaks CI-required in-memory path). | **Ratified** · 2026-09-10 | Plan v2 Stage 8.1 |
| ADR-105 | `ADR-105-tektos-reflection-synthesis-experience-engines.md` | **Ratified (2026-09-10)** · Stage 8.3 lands three new plugin subpackages `plugins/tektos/{reflection,synthesis,experience}/` as rewrites against `RelationalMemoryPort.write_narrative` (ADR-102 §D3) that preserve donor memory-tier semantics without dragging in `MemorySystem` (993 lines), `DreamtimeEngine`, `Hindsight`, or `LanguageGame`. Runtime-tier donor engines (`tektos-ultima/src/tektos/runtime/{reflection,synthesis,experience_replay}_engine.py`, 313 lines) rejected wholesale as orphan code (zero external consumers). **D1** three subpackages, engines constructor-injected with `relational_memory: RelationalMemoryPort | None = None` optional + memory-only ring buffer (max_records=100) fallback. **D2** rewrite memory-tier semantics (dataclass shapes `ReflectionInsight`/`ReflectionState`/`SynthesisResult`/`ExperienceRecord`, rule-based analytic logic `_generate_insight`/`_extract_lessons`/`_generate_recommendations`, domain-language docstrings verbatim as module preambles); reject runtime-tier orphan; defer `MemorySystem`/`Hindsight`/`Dreamtime` to Stage 13, `LanguageGame` to Stage 8.4, `SelfImprovementLoop` to Stage 8.7. **D3** `RelationalMemoryPort.write_narrative` as sink — no new port; `session_id` required, `agent_id="tektos.{reflection,synthesis,experience}"`, `title=f"{predicate}:{stable_slug}"`, `body` = JSON-serialised full record, `tags=(engine_name, category, session_id)`, `embedding=None` at 8.3, `confidence` in `(0,1]` port-validated, `provenance` per D4. Experience recall via `search_narratives(query=context, tags=("tektos.experience",), limit=N)`. **D4** nine locked constants (mirrors ADR-036 / ADR-052 pattern): `TEKTOS_{REFLECTION,SYNTHESIS,EXPERIENCE}_PROVENANCE` = `"tektos.{reflection,synthesis,experience}"`, `TEKTOS_{REFLECTION,SYNTHESIS}_PREDICATE` = `"tektos.{reflection,synthesis}.completed"`, `TEKTOS_EXPERIENCE_PREDICATE` = `"tektos.experience.recorded"`, `TEKTOS_{REFLECTION,SYNTHESIS,EXPERIENCE}_DEFAULT_CONFIDENCE = 0.75` (ADR-036 pre-Reflexion default). **D5** `TektosPlugin` dataclass grows three optional fields (`reflection`, `synthesis`, `experience`) mirroring ADR-104 D11 `turn_loop`; kernel best-effort setattr after boot. **D6** three independent kernel env-gates `KOSMOS_TEKTOS_{REFLECTION,SYNTHESIS,EXPERIENCE}={off,on}` (default `off` silent), unknown values raise, `on` requires `registry.relational_memory` non-None else ADR-101 degrade to `None` with WARN log; boot order `_boot_relational_memory → _boot_session → _boot_tektos_turn_loop → _boot_tektos_reflection → _boot_tektos_synthesis → _boot_tektos_experience → _boot_gnosis_seeder`. **D7** three new EventBus event types (envelope-first per ADR-023) `tektos.reflection.completed` / `tektos.synthesis.completed` / `tektos.experience.recorded`; `event_bus: EventBusPort | None = None` optional; `narrative_id` in payload is `None` when memory-only mode; publish failures caught + logged (fail-open, mirrors ADR-104 D2 pattern). **D8** FastAPI routers under `/tektos/api/{reflection,synthesis,experience}/*` (ADR-045 UI-parity): `POST /reflection/reflect`, `GET /reflection/recent`, `POST /synthesis/synthesize`, `GET /synthesis/recent`, `GET /experience/recent`, `POST /experience/record`; missing engine → `503 {"detail": "ADR-105 degrade: <engine> not wired", "adr": "ADR-105"}`. **D9** explicit deferrals: LLM-driven insight generation (Stage 8.4+), Reflexion (Stage 5.X per ADR-039), `SelfImprovementLoop` (Stage 8.7), `Hindsight`/`MemorySystem`/`Dreamtime` (Stage 13), `LanguageGame` (Stage 8.4), embedding generation (Stage 8.4+), turn-loop auto-reflection hook (Stage 8.4). No new formal port; no PORTING_LEDGER entry (ports semantics, not code). Rejects: fidelity-port memory tier (drags ~2500 lines of transitive donor infra violating Plan v2 stage assignment); fidelity-port runtime tier (orphan); `MemoryPort` instead of `RelationalMemoryPort` (wrong substrate — typed-edge triple store vs episodic narrative); new `ReflectionPort`/`SynthesisPort`/`ExperiencePort` (port inflation); required (non-None) engines (breaks Stage 3.13 test surface); single combined env-gate (parsing surface); auto-subscribe to `tektos.agent.turn.completed` at boot (violates ADR-007 spirit — Stage 8.4 concern). | **Ratified** · 2026-09-10 | Plan v2 Stage 8.3 |
| ADR-107 | `ADR-107-tektos-executor-and-tool-router.md` | **Ratified (2026-09-10)** · Stage 8.5 lands two sibling Tektos engine helpers under `plugins/tektos/executor/`: `TektosSpecExecutor` (consumes Stage 8.4 `BuildSpec`, produces `ExecutionRecord` via deterministic scaffolds + optional `SandboxPort.run`) + `TektosToolRouter` (routes `SubTask.tools_needed` from Stage 8.4 `DecompositionPlan` to a `ToolRoute` naming the chosen port + argument shape; routing only at 8.5, execution deferred). **D1** two sibling engines in one package (mirrors ADR-105 + ADR-106 shape). **D2** rewrite pattern (frozen slotted dataclasses over pydantic; donor `subprocess.run` → `SandboxPort.run` per ADR-082; donor scaffold heuristics preserved verbatim; donor tool-router capability table + keyword-match routing preserved verbatim; donor `execute_with_recovery` state machine + `ToolPerformance` mutable running stats REJECTED at 8.5). **D3** `RelationalMemoryPort.write_narrative` persistence sink for both. **D4** six locked constants (`TEKTOS_{EXECUTOR,TOOL_ROUTER}_{PROVENANCE,PREDICATE,DEFAULT_CONFIDENCE}=0.75`). **D5** `TektosPlugin` grows `executor: object \| None` + `tool_router: object \| None` fields. **D6** two `@_try` boot functions env-gated `KOSMOS_TEKTOS_{EXECUTOR,TOOL_ROUTER}={off,on}` default `off`; require `registry.relational_memory` non-None (ADR-101 degrade); executor additionally accepts optional `registry.sandbox` (falls open to `sandbox_unavailable` records when unwired). Shared `_boot_stage_8_x_engine` helper unchanged — now serves ADR-105 + ADR-106 + ADR-107. **D7** two new event types `tektos.executor.spec_executed` + `tektos.tool_router.routed` (envelope-first ADR-023). **D8** FastAPI routers `/tektos/api/{executor,tool-router}/*` with `503 ADR-107 degrade` on unwired. **D9** fail-open + explicit deferrals: recovery-strategy state machine (Stage 8.6 manager), `ToolPerformance` mutable stats (Stage 8.3 experience-replay covers), actual tool execution inside `TektosToolRouter` (Stage 8.6+), LLM-driven artifact synthesis (Stage 8.6+), MCP-based tool discovery (Stage 8.5+ later), integration with `TektosTurnLoop` (Stage 8.6+ manager), `RepoMapPort` (already vendored in-tree at Stage 4.7 · ADR-038; executor reads via existing `MemoryPort.query_temporal` seam — no new port surface). **D10** engines stay optional (mirrors ADR-104 D11 + ADR-105 D5 + ADR-106 D10). **D11** REJECTS `ExecutorPort` at 8.5 (single Tektos-internal consumer; `TektosPlugin.executor: object \| None` field IS the coupling surface; premature port surface). **D12** REJECTS `ToolRouterPort` at 8.5 (routing-only shape may change substantially when execution + recovery land at 8.6; locking a Protocol now would over-fit an interim design). No new formal port; PORTING_LEDGER updates: two new VENDORED rows (executor · ADR-107; tool_router · ADR-107). | **Ratified** · 2026-09-10 | Plan v2 Stage 8.5 |
| ADR-108 | `ADR-108-tektos-manager.md` | **Ratified (2026-09-10)** · Stage 8.6 lands `plugins/tektos/manager/` — the Tektos S3 manager. **D1** single-engine package `plugins/tektos/manager/` with `TektosManager` orchestrating lifecycle hooks (task-start/complete/error/spiral-update/rhythm) + health reporting. **D2** rewrite pattern (frozen slotted dataclasses over pydantic; enums as Literal unions; donor `agents/manager/models.py` + `archetype_tracker.py` + `guardrails.py` preserved verbatim; donor `metrics.py::PrimeMoverMetrics` REPLACED with pure `_check_threshold(name, value)` function + `ObservabilityPort.score()` delegation; donor `orchestrator.py::Manager._check_guardrails` REROUTED to `ImmunePort.scan()` with donor regex retained as fallback keyword scan when unwired). **D3** `RelationalMemoryPort.write_narrative` persistence sink (ADR-102). **D4** three locked constants (`TEKTOS_MANAGER_{PROVENANCE="tektos.manager",PREDICATE="tektos.manager.feedback_generated",DEFAULT_CONFIDENCE=0.75}`). **D5** `TektosPlugin` grows `manager: object \| None` field. **D6** bespoke `_boot_tektos_manager` (matches ADR-107 executor pattern — 4 optional collaborators: relational_memory + event_bus + immune + observability) env-gated `KOSMOS_TEKTOS_MANAGER={off,on}` default `off`; `on` requires `registry.relational_memory` non-None (ADR-101 degrade). **D7** three new event types `tektos.manager.{feedback_generated,archetype_recognized,guardrail_triggered}` (envelope-first ADR-023). **D8** FastAPI router `/tektos/api/manager/*` with `503 ADR-108 degrade` on unwired. **D9** fail-open + explicit deferrals: actual skill/tool creation on archetype threshold (deferred to future skill-registry stage; `Archetype.permanent_structure_id` stays `None`); rhythm scheduler (Stage 8.7+); recovery-strategy state machine execution (Stage 8.7 orchestrator; classification helper `classify_recovery` present at 8.6 discharges ADR-107 D9 point 1); `ToolPerformance` mutable running stats (delegated to `ObservabilityPort` — discharges ADR-107 D9 point 2 by delegation); TektosTurnLoop integration (Stage 8.7). **D10** engine stays optional (mirrors ADR-104 D11 + ADR-105 D5). **D11** REJECTS ManagerPort at 8.6 (single Tektos-internal consumer; `TektosPlugin.manager` field IS coupling surface). **R1** REJECTS donor `telemetry.py` (601 LOC RTX 5090 + pynvml + Unix-socket fan controller) — `ThermalPort` (ADR-081) owns RTX 5090 threshold table verbatim. **R2** REJECTS in-process `PrimeMoverMetrics.samples` accumulator — duplicates `ObservabilityPort` (ADR-025). **R3** REJECTS donor `_check_guardrails` inline regex as sole surface — `ImmunePort` (ADR-079) is the ratified scanner; regex kept only as fallback. **R4** REJECTS pydantic. | **Ratified** · 2026-09-10 | Plan v2 Stage 8.6 |
| ADR-109 | `ADR-109-tektos-ultima-api-gateway.md` | **Ratified (2026-09-24)** · Tektos integration Stage 9.1 lands `kernel/tektos_ultima_gateway.py` — kernel-side proxy from `/api/tektos-ultima/gateway/*` to the standalone Tektos API (`TEKTOS_ULTIMA_API_URL`, default `http://127.0.0.1:8020`). **D1** two routes: `GET …/gateway/health` (reachability probe) + `{GET,POST,PUT,PATCH,DELETE} …/gateway/{upstream_path:path}` transparent proxy (status/body/content-type passthrough). **D2** no registry coupling — mounts unconditionally, per-request typed degrade: 503 `tektos_ultima_unavailable` on connect failure, 502 `tektos_ultima_proxy_error` on proxy fault; kernel stays healthy. **D3** hop-by-hop header stripping; `Response` (not `JSONResponse`) so non-JSON upstream bodies survive. **D4** SSE: `text/event-stream` forwarded chunk-by-chunk via `client.send(req, stream=True)`; the `httpx.AsyncClient` is generator-owned (closed in `_forward`'s `finally` on stream end or browser disconnect); mid-stream drop emits one terminal `event: proxy_error` frame. **D5** reuses the ADR-091 bridge env var `TEKTOS_ULTIMA_API_URL`, read per-request. **D6** 7 contract tests against a real fake upstream (uvicorn on random port), not mocks. ADR-091 iframe bridge + CSP middleware stay mounted until Stage 9.5 parity retires them; Stages 9.2–9.4 (native pages) build on this gateway; engine porting remains a later phase. No new formal port. | **Ratified** · 2026-09-24 | Tektos integration Stage 9.1 |
| ADR-110 | `ADR-110-tektos-ultima-native-dashboard.md` | **Ratified (2026-09-24)** · Tektos integration Stage 9.2 lands the native `/tektos-ultima` dashboard: 14-card subsystem status grid (immune, thermal, inference, memory, rag, skills, tools, models, plugins, neo4j, postgres, redis, hindsight, self-repair) driving the Tektos API exclusively through the ADR-109 kernel gateway. **D1** `/tektos-ultima` becomes the native page; the ADR-091 iframe moves to `/tektos-ultima/legacy` (behaviour unchanged, `-legacy-*` test-ids); bridge router + CSP middleware stay until Stage 9.5. **D2** same-origin data path — no direct `:8020` calls; upstream outage degrades to the gateway 503 envelope → offline banner + per-card `degraded`, never a crash (ADR-101 spirit at the UI layer). **D3** per-card pure parse rules over documented upstream shapes; unexpected shape → `down: unexpected shape` (resilient to schema drift). **D4** 10 s `Promise.all` fan-out with in-flight guard; header pill aggregates healthy/total + upstream URL + LLM model + active sessions from the ADR-109 health probe. **D5** existing oklch design tokens, dark theme. **D6** `ui/tests/20-tektos-ultima-shell.spec.ts` rewritten: 4 dashboard tests (CI-safe typed-envelope assertion) + legacy iframe contract + 4 preserved ADR-091 bridge/CSP tests; 8/8 green against the live kernel. Explicit deferrals: sessions/chat (9.3), ops pages (9.4), iframe retirement (9.5). No new kernel routes, no new formal port. | **Ratified** · 2026-09-24 | Tektos integration Stage 9.2 |
| ADR-106 | `ADR-106-tektos-spec-planner-and-task-decomposer-engines.md` | **Ratified (2026-09-10)** · Stage 8.4 lands two new Tektos engine slices absorbing donor `tektos-ultima/src/tektos/agents/planner/` (5-stage NL→spec pipeline: language-game → disambiguator → translator → template-selector → spec-generator) as `plugins/tektos/planner/spec_planner.py::TektosSpecPlanner` + donor `runtime/task_decomposer.py` as `plugins/tektos/decomposer/engine.py::TaskDecomposer`. **D1** two sibling engines — spec-planner extends existing package (Stage 4.7 `TektosTurnPlanner` seed unchanged); decomposer new package. **D2** rewrite pattern (frozen slotted dataclasses over pydantic; RelationalMemoryPort.write_narrative sink over in-memory dict); donor rule-based analytic core preserved verbatim (domain signatures, ambiguity dictionary, filler/vague-term maps, four architecture templates, five decomposer branches). **D3** RelationalMemoryPort.write_narrative persistence for both. **D4** six locked constants (`TEKTOS_{SPEC_PLANNER,DECOMPOSER}_{PROVENANCE,PREDICATE,DEFAULT_CONFIDENCE}=0.75`). **D5** `TektosPlugin` grows `spec_planner: object | None` + `decomposer: object | None` fields (naming rationale: avoids collision with `plugins.tektos.planner` package). **D6** two `@_try` boot functions env-gated `KOSMOS_TEKTOS_{SPEC_PLANNER,DECOMPOSER}={off,on}` default `off`; require `registry.relational_memory` non-None (ADR-101 degrade). **D7** two new event types `tektos.planner.spec_generated` + `tektos.decomposer.plan_generated` (envelope-first ADR-023). **D8** FastAPI routers `/tektos/api/{spec-planner,decomposer}/*` with `503 ADR-106 degrade` on unwired. **D9** fail-open + `LanguageGame` enum lands NOW at 8.4 (discharges ADR-105 D9 forward-looking deferral); ExperienceRecord.context accepts optional `language_game` by convention (no schema change). **D10** engines stay optional (mirrors ADR-104 D11 + ADR-105 D5). **D11** REJECTS PlannerPort at 8.4 (no cross-plugin consumer; `spec_planner: object \| None` field IS the coupling surface; port surface would be near-empty; PORTING_LEDGER PLANNED row names LLMPort/EventBusPort/RepoMapPort — no PlannerPort). **D12** REJECTS LLMPort at 8.4 (donor pipeline 100% rule-based — verified zero LLM calls in language_game/disambiguator/translator/template_selector/spec_generator/decomposer; forcing LLM adapter dep would break DoD when adapters unhealthy). Explicit deferrals (D9): LLM-driven translator/disambiguator/spec_generator quality lift (Stage 8.4+1 after ADR-087 role-routing), `RepoMapPort` (Stage 8.5 executor helpers), filesystem-reading tools (Stage 8.5), Reflexion actor-evaluator confidence per ADR-039 (Stage 5.X), interactive user-question loop for ClarifyingQuestion (Stage 8.5+ frontend), real BuildSpec execution against TektosTurnLoop (Stage 8.5), MCP-based tool discovery in decomposer tools_needed (Stage 8.5). No new formal port; PORTING_LEDGER updates: Tektos planner PLANNED row (Stage 4.7+1) flips to VENDORED; task-decomposer VENDORED row added. | **Ratified** · 2026-09-10 | Plan v2 Stage 8.4 |
| ADR-090 | `ADR-090-self-modification-port-deferred.md` | **PROPOSED (DEFERRED, 2026-09-10)** · `SelfModificationPort` surface proposed (`propose` / `approve` / `apply` / `rollback` / `list_pending`) but **not ratified** in Stage 1. Interim behaviour (Stage 5.6): self_improve/self_repair sub-packages land at `plugins/tektos/self_*/` gated by `ApprovalPort` — propose-only, no filesystem mutation; every proposal writes `MemoryPort` (`provenance="tektos_self_modification"`, `confidence ≤ 0.9`). Ratification pre-conditions: ImmunePort SelfModificationDetector green + LoopSafetyPort interlocks proven + end-to-end rollback demonstrated + explicit user approval. | **Proposed · DEFERRED** · 2026-09-10 | TBD (earliest Stage 5.6+1 session) |

## Remaining open decisions

**As of 2026-09-10, two ADRs are deferred: ADR-090 (SelfModificationPort) and TTS engine selection (per ADR-097 D3, targeted for Stage 6.5+1).** All other decisions are Ratified or Locked. ADR-010 landed `LOCKED` after the Stage 6.2 head-to-head. Stage 1 landed ADR-077 through ADR-090 (14 ADRs); Stage 2 lands ADR-091 (microfrontend shell integration); Stage 3 lands ADR-092 (Tektos runtime absorption scope); Stage 4.7 lands ADR-093 (Tektos sandbox + planner + tool-registry absorption scope); Stage 4.8 lands ADR-094 (Tektos tool-surface reconciliation + filesystem tools + path-traversal detector); Stage 5.6 lands ADR-095 (self-improvement + self-repair propose-only scope); Stage 6.5 lands ADR-096 (voice + vision scope + two-write pattern), ADR-097 (VoicePort adapter selection: faster-whisper + TTS deferred), and ADR-098 (VisionPort adapter selection: Qwen2.5-VL via Ollama + Tesseract); Stage 7.4 lands **ADR-099** (Stage 7.4 re-scope — H1 skipped; `MemoryPort.search_hybrid` lands on `DozerDbMemoryAdapter` with RRF fusion + `LexicalIndex` adapter-scoped Protocol + `InMemoryLexicalIndex` BM25 test backend); Stage 7.4+1 lands **ADR-100** (production `DozerDbLexicalIndex` backed by DozerDB/Neo4j Lucene fulltext, bootstrapped lazily via `CREATE FULLTEXT INDEX ... IF NOT EXISTS`, single-`text`-property Cypher shape, post-YIELD corpus filter, minimal-payload rehydration, log-only degradation on Lucene failure, boot-time wiring deferred to Stage 7.4+2); Plan v2 Stage 8.3 lands **ADR-105** (Tektos reflection + synthesis + experience-replay engines — three plugin subpackages routed through `RelationalMemoryPort.write_narrative`); Plan v2 Stage 8.0 lands **ADR-102** (`RelationalMemoryPort` — new formal port (23rd), Postgres 18 + pgvector 0.8.1 + pg_uuidv7 as 5th memory layer combining R1 audit-ledger + R2 episodic-narrative-store; two adapters aiosqlite-noop + asyncpg-postgres; Alembic migrations; env-gated kernel wiring with ADR-101 degrade pattern; discharges the audit-flagged 5th-layer Postgres role decision).

Stage 7.4+2 lands **ADR-101** (kernel-boot wiring of `DozerDbLexicalIndex` via opt-in `KOSMOS_MEMORY_LEXICAL={off,dozerdb}` env-gate inside `kernel/app.py::_boot_memory`; shared Bolt endpoint with the graph backend; boot-time `is_healthy()` check with fail-closed fall-through to `lexical=None`; factory signature unchanged — Zetesis inherits the wired lane transparently; discharges ADR-100 D6 deferral pointer). ADR-090 remains PROPOSED pending its named ratification pre-conditions; TTS engine selection remains open pending a post-Stage-6.5 Coqui-fork benchmark.

## Amending an ADR

1. Never edit a Ratified ADR in place except to add a `> **STATUS AMENDMENT:**` block at the top.
2. Amendments require a `BUILD_LOG.md` entry (timestamp + reason).
3. If the amendment reverses the decision, author a new ADR that supersedes the old one and mark the old one `Amended · superseded by ADR-###`.
4. The `kosmos-adr-authoring` Perplexity Computer skill enforces this workflow.